# Data Nexus 后续开发规划（2026 年 08 月 04 日）

本文档记录 2026 年 08 月 04 日之后的未完成开发任务及建议执行顺序。所有未完成任务统一
使用 `- [ ]`；已完成内容只作为基线说明，不使用待办标记。

## 0. 项目与执行约束

Data Nexus 是 Rust workspace。`data-proxy/` 是历史目录名，不代表另一个项目；项目不使用
仓颉工具链。Rust 工具链固定为 `1.94.1`，以 `data-proxy/rust-toolchain.toml` 为准。

所有编译、测试、smoke、SQL 验收、覆盖率、日志和临时导出物必须位于：

```text
/Volumes/fushilu/.caches/data-nexus/
```

- [ ] 所有 Cargo 命令在 `data-proxy/` 中执行，并设置
  `CARGO_TARGET_DIR=/Volumes/fushilu/.caches/data-nexus/cargo-target`。
- [ ] 每个独立功能使用独立 Git commit，提交标题包含任务 ID。
- [ ] 每个新增 SQL case 使用独立 `.sql` 文件，不把 SQL 文本嵌入 JSON manifest。
- [ ] 每个 SQL 文件保留 `case`、`Purpose`、`Expected`、`Dialect` 四行注释。
- [ ] 每个 SQL case 在 manifest 中明确方言、frontend、backend、协议、策略结果和副作用。
- [ ] 所有预期结果只使用 `allow`、`rewrite`、`deny`、`unsupported`，禁止 `unknown`。
- [ ] 所有 skip 必须包含 `reason`、`issue` 和 `expires_when`。
- [ ] 完成任务后更新 `todo-impl.md`，并保持 `todo.md` 的未完成状态诚实。
- [x] 修正 `todo-impl.md` 中 SQLT-3B3 的状态：实际提交是 `26aae14`，不再写“待提交”。

## 1. 当前 SQLT 基线

当前 manifest 共 272 条：metadata 20、DQL 86、DML 43、DDL 57、TCL 11、cursor 24、invalid 21、
unsupported 9、security 1。Security 专项不计入 canonical 家族的最低预算；TCL/prepared/cursor
已达到 35/35，非法/恶意/unsupported 已达到 30/30。

已完成的 SQLT 提交：

- `cdab3d6`：SQLT-1 capability registry、manifest 和 validator。
- `c0b2831`：SQLT-2 固定版本 Docker fixture 和 backend oracle。
- `6288a6c`：SQLT-3A metadata/session/diagnostic 20 条。
- `3c66509`：SQLT-3B1 基础 DQL。
- `6d1914c`：SQLT-3B2 JOIN、子查询、CTE、集合与窗口。
- `26aae14`：SQLT-3B3 函数、类型、日期、JSON、正则；Docker 120/120 通过。
- `2617366`：SQLT-3C1 INSERT、值类型、约束错误和精确状态 oracle；Docker 22/22 通过。
- `f234439`：SQLT-3C2 UPDATE、DELETE、子查询、方言写法和 affected rows；Docker
  单 tranche 28/28、C1+C2 合并 50/50 通过。
- `7adb345`：SQLT-3C3a PostgreSQL/MySQL 冲突写入、INSERT IGNORE、REPLACE。
- `b9e401a`：SQLT-3C3b PostgreSQL RETURNING、MERGE、data-modifying CTE。
- `b104033`：SQLT-3C3c savepoint 回滚和约束错误后同连接恢复；完整 DML Docker 64/64。
- `e73fde0`：SQLT-3B4a 方言排序、wildcard、array、JSON 与日期范围；完整 DQL Docker
  141/141。
- `57e1c67`：SQLT-3B4b 双连接行锁语义；direct/gateway 完整锁矩阵 16/16。
- `8ab72e7`：SQLT-3B4c 1 MiB 长字段、10,000 行结果与 64 KiB 流式摘要；完整边界矩阵
  8/8。
- `1bbf74b`：SQLT-3D1a 普通/幂等/重复建表语义、精确 catalog/error oracle 和 DDL runner；
  固定 Docker direct/security-off gateway 16/16。
- `9661dee`：SQLT-3D1b ADD/DROP/RENAME COLUMN、双方言列类型变更和 table rename；
  完整 DDL Docker direct/security-off gateway 36/36。
- `79c1f39`：SQLT-3D1c1 DROP、幂等 DROP 和 TRUNCATE 数据探针；完整 DDL Docker 48/48，
  TRUNCATE 四条路径均为 3 行归零且 catalog 不变。
- `6123c7f`：SQLT-3D1c2 临时表同连接可见、第二连接隔离和断线回收；双方言
  direct/security-off gateway 多连接 Docker 4/4。
- `7eaafbe`：SQLT-3D2a1 PRIMARY KEY 增加/删除生命周期、精确约束 metadata oracle，以及
  普通 DDL runner 对专用 session case 的显式筛选；完整 DDL Docker 56/56。
- `9190f1b`：SQLT-3D2a2 UNIQUE/FOREIGN KEY/CHECK、隐式唯一索引与引用 metadata，以及
  CHECK 违反写入的稳定错误/零副作用探针；普通 DDL 68/68、临时表 4/4。
- `fc6d69a`：SQLT-3D2b1 DEFAULT 与双方言 NOT NULL 设置/删除生命周期，精确列 metadata 与
  数据不漂移 oracle；普通 DDL 84/84、临时表 4/4。
- `7720c55`：SQLT-3D2b2 UNIQUE/FOREIGN KEY/CHECK/NOT NULL 脏数据冲突，固定错误 identity，
  并证明失败后 catalog、约束 metadata 和原始数据均不变；普通 DDL 100/100、临时表 4/4。
- `4fed1c6`：SQLT-3D2c 普通/唯一复合索引、双方言 DROP INDEX 和重复索引名错误；精确验证
  索引名称、列序、唯一性与失败无副作用，普通 DDL 116/116、临时表 4/4。
- `906bb83`：SQLT-3D3a CREATE/DROP VIEW 生命周期、重复创建、缺失依赖和缺失对象错误；精确
  验证 VIEW 对象/投影列 metadata 与失败无副作用，普通 DDL 136/136、临时表 4/4。
- `3426b19`：SQLT-3D3b PostgreSQL schema 生命周期、重复创建、非空删除和缺失对象错误；精确
  验证 namespace owner、内部对象/列 metadata 与失败无副作用，普通 DDL 146/146、临时表 4/4。
- `63884c1`：SQLT-3D3c PostgreSQL sequence CREATE/ALTER/DROP、重复创建和缺失对象错误；精确
  验证 owner、参数、缓存运行状态与失败无副作用，普通 DDL 158/158、临时表 4/4。
- `2c685ca`：SQLT-3D3d MySQL database 权限边界；受限 `sqlt` 账号的 CREATE/DROP/ALTER
  DATABASE、独立权限 oracle 与 direct/security-off gateway runner，专用矩阵 10/10。
- `07a4a5a`：SQLT-3E1 双方言事务生命周期、错误恢复、隔离级别和 DDL 事务差异；TCL
  20/20 case-dialect、40/40 direct/gateway path。
- `421eeb7`：SQLT-3E2 MySQL binary prepared 生命周期；修复原始 bytes、无响应
  `COM_STMT_CLOSE` 和 schema change 后重新 prepare metadata，真实 Docker 8/8 case、16/16 path。
- `8f38e85`：SQLT-3E3 PostgreSQL extended wire 生命周期；覆盖 statement/portal、re-Bind、分页、
  binary result、错误后 Sync 恢复和 ReadyForQuery I/T/E，真实 Docker 8/8 case、16/16 path。
- `45cce8f`：SQLT-3E4 PostgreSQL cursor 生命周期 corpus、oracle、raw-wire 客户端和 Docker runner；
  后续由 `dc7c8b9`、`dfde63a`、`f14440a` 修复 command tag、多行 DECLARE 和 backend loss fail-closed。
- `7f599b7`：SQLT-3F1 新增 12 条确定性错误 case、精确 vendor error identity、零副作用 probe、
  invalid corpus Docker runner 与 validator/selector 单测；direct/security-off gateway 44/44 路径通过。
- `1481a1b`：SQLT-3F2 新增 8 条协议、词法与固定资源边界 case、真实 binary/extended wire driver、
  精确 boundary oracle 与安全 frame decode；固定 Docker direct/security-off gateway 24/24 路径通过。
- `de7037e`、`6c64a14`、`857f551`：SQLT-3F3 危险 SQL capability scanner、backend execute 前置
  fail-closed guard、低基数审计和 9 条 unsupported corpus；固定 Docker direct/security-off gateway
  24/24 path，17/17 gateway step 审计闭合且无敏感 SQL 泄漏。

剩余最低缺口：

| 家族 | 当前 | 最低目标 | 尚缺 |
| --- | ---: | ---: | ---: |
| 会话、元数据、诊断 | 20 | 20 | 0 |
| DQL | 86 | 80 | 0 |
| DML | 43 | 35 | 0 |
| DDL | 57 | 30 | 0 |
| TCL、prepared、cursor | 35 | 35 | 0 |
| 非法、恶意、unsupported | 30 | 30 | 0 |
| **六大家族合计** | **271** | **230** | **0** |

## 2. 当前执行顺序

- **SQLT-3C1 已完成**：基础 INSERT、值类型和约束错误；固定 Docker 验收 22/22 通过。
- **SQLT-3C2 已完成**：UPDATE、DELETE、子查询和方言写法；固定 Docker
  单 tranche 28/28、C1+C2 合并 50/50 通过。
- **SQLT-3C3a 已完成**（`7adb345`）：双方言冲突写入、MySQL IGNORE/REPLACE；聚焦
  Docker 5/5、完整 DML 回归 55/55 通过。
- **SQLT-3C3b 已完成**（`b9e401a`）：PostgreSQL RETURNING、MERGE、data-modifying CTE；
  聚焦 Docker 5/5、完整 DML 回归 60/60 通过。
- **SQLT-3C3c 已完成**（`b104033`）：savepoint 回滚与错误后同连接恢复；聚焦 Docker 4/4、
  完整 DML 回归 64/64 通过。
- **SQLT-3B4a 已完成**（`e73fde0`）：DQL 新增 `065`～`080`，聚焦 Docker 21/21、完整
  DQL 回归 141/141 通过。
- **SQLT-3B4b 已完成**（`57e1c67`）：DQL 新增 `081`～`084` 和双连接锁 runner，direct
  8/8、gateway 8/8、默认完整 Docker 锁回归 16/16 通过。
- **SQLT-3B4c 已完成**（`8ab72e7`）：DQL 新增 `085`～`086` 和 64 KiB chunk 流式摘要
  runner，direct 4/4、gateway 4/4、默认完整 Docker 边界回归 8/8 通过。
- **SQLT-3B4 已完成**（`e73fde0`、`57e1c67`、`8ab72e7`）：DQL 从 64 条扩展至 86 条；
  普通 DQL 141/141、锁 DQL 16/16、边界 DQL 8/8。
- **SQLT-3D3a 已完成**（`906bb83`）：DDL 新增 `037`～`041`，覆盖双方言 VIEW 生命周期及
  三类稳定失败；聚焦 Docker 20/20、普通 DDL 136/136、临时表 4/4。
- **SQLT-3D3b 已完成**（`3426b19`）：DDL 新增 `042`～`046`，覆盖 PostgreSQL schema
  生命周期、重复创建、非空删除与缺失对象；聚焦 Docker 10/10、普通 DDL 146/146、临时表 4/4。
- **SQLT-3D3c 已完成**（`63884c1`）：DDL 新增 `047`～`052`，覆盖 PostgreSQL sequence
  生命周期、参数/restart 演进、重复创建与缺失对象；聚焦 Docker 12/12、普通 DDL 158/158、
  临时表 4/4。
- **SQLT-3D3d 已完成**（`2c685ca`）：DDL 新增 `053`～`057`，覆盖受限 MySQL 账号的
  `CREATE/DROP/ALTER DATABASE` 与 `IF EXISTS/IF NOT EXISTS` 权限边界；专用 Docker 10/10、
  普通 DDL 158/158、临时表 4/4。
- **SQLT-3E1 已完成**（`07a4a5a`）：TCL 从 1 条扩展到 11 条；双方言事务、savepoint、
  错误恢复、read-only、隔离级别和 DDL/DML 混合事务 Docker 20/20、40/40 path。
- **SQLT-3E2 已完成**（`421eeb7`）：新增 8 条真实 MySQL binary prepared case；固定 connector
  Docker 的 direct/security-off gateway 验收 8/8、16/16 path。
- **SQLT-3E3 已完成**（`8f38e85`）：新增 8 条 PostgreSQL extended-wire case；固定 PostgreSQL
  16.8 Docker 的 direct/security-off gateway 验收 8/8、16/16 path。
- **SQLT-3E4 已完成**（`45cce8f`、`dc7c8b9`、`dfde63a`、`f14440a`）：复验基础 cursor 并新增
  7 条生命周期与清理 case；固定 PostgreSQL 16.8 Docker 验收 8/8、18/18 path。
- [x] **SQLT-3D1～3D3**：DDL 从 1 条扩展到 57 条，普通 DDL 与专用 session/database runner
  分离验收。
- [x] **SQLT-3E**：TCL、prepared、extended wire、cursor 已扩展到 35 条。
- [x] **SQLT-3F**：非法、恶意和 unsupported 已扩展到 30 条；3F3 由 `de7037e`、`6c64a14`、
  `857f551` 完成，危险 capability Docker 24/24 与全套相邻回归通过。
- [ ] **SQLT-4**：协议与跨协议矩阵。
- [ ] **SQLT-5**：治理策略、审计和故障矩阵。
- [ ] **SQLT-6**：CI 分层门禁和结构化报告。
- [ ] **A10**：backend PostgreSQL `DECLARE ... WITH HOLD`。
- [ ] **A08**：PostgreSQL wire 透传和 backend TLS 完整性。
- [ ] **A09**：Portal 端到端流式。
- [ ] **A06**：Backend 到 PEP 的精确窗口内存证明。
- [ ] **H05、B08、T01**：P1 产品重债。

## 3. SQLT-3C1：基础 INSERT corpus

目标是建立可重复的 DML 状态 oracle，优先新增约 10～12 条 case。

### SQLT-3C1 开发方案（2026-08-04）

1. 新增 12 条 canonical INSERT case：单行显式乱序列、多行、`NULL`、`DEFAULT`、精确
   decimal、MySQL/PostgreSQL 特殊文本、`INSERT ... SELECT`，以及主键、外键、NOT NULL、
   VARCHAR 长度错误。
2. 为 `sqlt_mutations` 增加带默认值的 `status` 列，使 DEFAULT 语义可以通过最终状态验证；
   保持现有 schema、seed 和 oracle 的其他行为不变。
3. 新增 `dml-oracles.json` 保存每个 case、每个方言的执行类型、精确状态输出和稳定错误身份；
   SQL 语句只保存在独立 `.sql` 文件中，不嵌入 JSON。
4. 新增双方言状态查询 SQL，并由 validator 强制检查 DML oracle 与 manifest 一一对应、字段闭合、
   oracle SQL 路径安全、错误 case 必须声明 error identity。
5. 新增可用 `SQLT_DML_CASE_FROM` / `SQLT_DML_CASE_TO` 限定范围的 Docker runner；每个
   `case × dialect` 执行前重建 fixture，成功 case 比较精确最终状态，失败 case 同时比较错误身份
   和执行前后状态，证明失败语句没有部分写入。
6. 使用固定 `mysql:8.0.42`、`postgres:16.8` 完成静态校验、Python 单测、shell 语法检查和
   双方言真实执行；全部产物写入 `/Volumes/fushilu/.caches/data-nexus/sql-matrix/<run-id>/`。

- [x] 增加单行 `INSERT ... VALUES` case。
- [x] 增加多行 INSERT case。
- [x] 增加显式列列表和不同列顺序 case。
- [x] 增加显式 `NULL` case。
- [x] 增加默认值 case；MySQL/PostgreSQL 方言不同则拆分文件。
- [x] 增加精确 decimal case，验证写入后数值没有精度丢失。
- [x] 增加空字符串、引号、反斜杠和 UTF-8 文本 case。
- [x] 增加 `INSERT ... SELECT` case。
- [x] 增加主键重复错误 case。
- [x] 增加外键错误 case。
- [x] 增加 NOT NULL 约束错误 case。
- [x] 增加字符串长度错误 case。
- [x] 为成功 case 保存执行后状态 oracle，而不只检查退出码或 affected rows。
- [x] 为失败 case 比较 MySQL error number/SQLSTATE 与 PostgreSQL SQLSTATE。
- [x] 证明失败 INSERT 不留下部分写入或其他 backend 副作用。
- [x] 新增可按 DML case ID 范围执行的固定版本 Docker runner。
- [x] 使用 `mysql:8.0.42` 和 `postgres:16.8` 完成双方言 22/22 验收。
- [x] 将全部验收产物写入
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/dml-full-final-20260804/`。
- [x] MySQL direct 与 gateway 客户端显式使用 `utf8mb4`，并验证 UTF-8 文本精确落库。
- [x] 独立提交 `SQLT-3C1`：`2617366`。

## 4. SQLT-3C2：UPDATE 和 DELETE corpus

目标是新增 16 条 canonical case（`SQLT-DML-015`～`SQLT-DML-030`），覆盖 UPDATE/DELETE
的单行、多行、零行、表达式、子查询、双方言 JOIN 写法及稳定约束错误。

### SQLT-3C2 开发方案（2026-08-04）

1. 先扩展双方言 seed，为 UPDATE/DELETE 提供相同、可重复的 mutation 行；状态查询继续使用独立
   oracle SQL，并以主键排序输出完整最终状态。
2. `SQLT-DML-015`～`021` 覆盖单行、多行、decimal 表达式、写入 NULL、零行、标量子查询和
   相关子查询 UPDATE；`SQLT-DML-022`～`023` 分别保存 MySQL JOIN UPDATE 与 PostgreSQL
   `UPDATE ... FROM`，不在同一 SQL 文件中混入方言分支。
3. `SQLT-DML-024`～`028` 覆盖单行、多行、零行、子查询和方言 JOIN/USING DELETE；若双方言
   语法不同，使用独立 SQL 文件和 manifest 方言声明。
4. `SQLT-DML-030` 覆盖 UPDATE 约束失败，记录 MySQL error number/SQLSTATE 与 PostgreSQL
   SQLSTATE，并断言失败前后状态完全一致。
5. 扩展 `dml-oracles.json` 和 validator，使 INSERT 与 UPDATE/DELETE tranche 都要求逐 case、
   逐方言 oracle；成功 case 比较精确最终状态，runner 同时记录并比较 direct/gateway affected rows。
6. 无 `WHERE` 的策略拒绝不混入 security-off backend 语义 tranche，移至 SQLT-5 治理策略矩阵；
   SQLT-3C2 只验证数据库语义，避免把安全策略未启用误写成数据库不支持。
7. 先按 ID 范围运行聚焦 Docker case，再使用固定 `mysql:8.0.42`、`postgres:16.8` 完整验收；
   所有构建和运行产物继续写入 `/Volumes/fushilu/.caches/data-nexus/`。

- [x] 增加带顶层 `WHERE` 的单行 UPDATE。
- [x] 增加多行 UPDATE。
- [x] 增加表达式更新和 decimal 运算更新。
- [x] 增加写入 `NULL` 的 UPDATE。
- [x] 增加零行受影响 UPDATE。
- [x] 增加标量子查询 UPDATE。
- [x] 增加相关子查询 UPDATE。
- [x] 增加 MySQL JOIN UPDATE 方言 case。
- [x] 增加 PostgreSQL `UPDATE ... FROM` 方言 case。
- [x] 增加带 `WHERE` 的单行 DELETE。
- [x] 增加多行 DELETE。
- [x] 增加零行受影响 DELETE。
- [x] 增加子查询 DELETE。
- [x] 增加 MySQL JOIN DELETE 方言 case。
- [x] 增加 PostgreSQL `DELETE ... USING` 方言 case。
- [ ] 在 SQLT-5 增加无 `WHERE` UPDATE/DELETE 的策略拒绝 case。
- [x] 增加 UPDATE 外键冲突 case，并验证双方言稳定错误身份和无副作用。
- [x] 对每条成功写操作比较 direct/gateway affected rows 和完整最终表状态。
- [ ] 在 SQLT-3C3 显式事务 case 中比较事务状态和后续命令恢复。
- [ ] 对 deny/ticket 路径证明 backend 无副作用。
- [x] 使用固定版本 Docker 完成精确状态 oracle 验收：SQLT-3C2 28/28、合并回归 50/50。
- [x] 回归 SQLT-2 backend oracle：10/10 通过。
- [x] 独立提交 `SQLT-3C2`：`f234439`。

## 5. SQLT-3C3：upsert、RETURNING 和方言 DML

### SQLT-3C3 开发方案（2026-08-05）

1. **SQLT-3C3a（`SQLT-DML-031`～`035`）**：分别增加 PostgreSQL `ON CONFLICT DO
   NOTHING`、`ON CONFLICT DO UPDATE`，以及 MySQL `ON DUPLICATE KEY UPDATE`、
   `INSERT IGNORE`、`REPLACE`。每条 SQL 保持单文件和四行注释，比较精确最终状态与固定客户端
   affected rows，先做聚焦 Docker 验收，再回归完整 DML corpus。
2. **SQLT-3C3b（`SQLT-DML-036`～`040`）**：增加 PostgreSQL INSERT/UPDATE/DELETE
   `RETURNING`、`MERGE` 和 data-modifying CTE。oracle 新增精确返回行字段；runner 分别比较
   direct/gateway 的返回行、最终状态和适用的 command-tag affected rows。MySQL 不支持的语法不
   伪造跨方言 case，而由 manifest 的 PostgreSQL 方言、frontend 和 backend 声明限定范围。
3. **SQLT-3C3c（`SQLT-DML-041`～`043`）**：增加双方言 savepoint 成功后回滚，以及分别保存
   MySQL/PostgreSQL 错误后恢复脚本。manifest 使用 `statement_mode=script` 和
   `transaction_mode=explicit`；oracle 比较稳定错误身份、恢复后的完整状态及后续命令产生的
   可观察标记，证明同一连接完成恢复。
4. validator 不再通过 case ID 猜测 oracle 类型：成功 case 可声明 `affected_rows`、
   `returned_rows` 或显式事务状态契约，失败恢复 case 同时允许稳定 `error` 和最终 `state`。
   normalizer 必须拒绝缺失或重复的 command tag，避免宽松文本匹配形成假通过。
5. 固定后端继续使用 `mysql:8.0.42` 和 `postgres:16.8`；每个 case 都重置 fixture，并同时运行
   backend direct 与 security-off gateway。所有构建、容器日志、原始响应和归一化产物写入
   `/Volumes/fushilu/.caches/data-nexus/`。
6. allow/deny/ticket 属于治理策略副作用矩阵，继续登记在 SQLT-5；SQLT-3C3 不在 security-off
   runner 中伪验收策略结果。SQLT-5 必须复用这些 DML case 证明 deny/ticket 后 backend 无副作用。
7. 三个切片各自完成静态校验、Python 单测、聚焦 Docker 和 DML 合并回归后独立提交；最后再以
   单独文档提交登记真实 commit ID。

- [x] 增加 PostgreSQL `ON CONFLICT DO NOTHING`。
- [x] 增加 PostgreSQL `ON CONFLICT DO UPDATE`。
- [x] 增加 PostgreSQL INSERT `RETURNING`。
- [x] 增加 PostgreSQL UPDATE `RETURNING`。
- [x] 增加 PostgreSQL DELETE `RETURNING`。
- [x] 增加 MySQL `ON DUPLICATE KEY UPDATE`。
- [x] 增加 MySQL `INSERT IGNORE`。
- [x] 增加 MySQL `REPLACE`。
- [x] 增加 PostgreSQL `MERGE`。
- [x] 增加 PostgreSQL CTE 中 DML；由 manifest 限定 PostgreSQL frontend/backend，不伪造
  MySQL 跨方言 case。
- [x] 增加 savepoint 中 DML 成功后回滚 case。
- [x] 增加 DML 错误后的事务状态和后续命令恢复 case。
- [ ] 在 SQLT-5 增加 ticket、allow、deny 三种治理结果及副作用断言。
- [x] 将 DML canonical corpus 补到至少 35 条：当前 43 条。
- [x] 使用固定版本 Docker 完成双方言状态和返回结果验收：最终 64/64 通过。
- [x] 独立提交 `SQLT-3C3`：`7adb345`、`b9e401a`、`b104033`。

## 6. SQLT-3B4：DQL 收口到 80 条

### SQLT-3B4 开发方案（2026-08-05）

1. **SQLT-3B4a（`SQLT-DQL-065`～`080`）**新增 16 条确定性查询，将 DQL 从 64 条补到
   80 条：`065`～`068` 覆盖 PostgreSQL ILIKE/NULLS FIRST/NULLS LAST 和 MySQL 等价 NULL
   排序；`069`～`072` 覆盖 qualified/alias wildcard、嵌套 alias 遮蔽和重复列名；`073`～`074`
   覆盖 PostgreSQL array 构造/索引/包含；`075`～`077` 覆盖双方言嵌套 JSON 路径和 PostgreSQL
   JSON 数组；`078`～`080` 覆盖双方言 interval 与 timestamp 范围。
2. 每条 case 保持独立 SQL 文件与四行注释；方言语法不同时使用独立文件，不在 SQL 中加入客户端
   条件分支。所有多行结果使用完整排序键，oracle 比较 LF/TSV、`NULL` 标记、精确值和行顺序。
3. **SQLT-3B4b（`SQLT-DQL-081`～`084`）**覆盖 `FOR UPDATE`、`FOR SHARE`、`NOWAIT` 和
   `SKIP LOCKED`。新增独立锁语义 runner，使用两个真实数据库连接：连接 A 持锁，连接 B 证明
   NOWAIT 的稳定错误身份或 SKIP LOCKED 的精确集合，随后验证 rollback/连接回收。
4. 普通 DQL 继续由固定 `mysql:8.0.42`、`postgres:16.8` runner 验收；锁 runner 同时测试
   backend direct 与 security-off gateway，所有原始响应、连接日志和归一化结果写入
   `/Volumes/fushilu/.caches/data-nexus/`。
5. 先跑 `065`～`080` 聚焦 Docker 与完整 DQL 回归并独立提交 SQLT-3B4a；再完成锁 runner、
   聚焦/完整回归并独立提交 SQLT-3B4b；最后单独提交真实 commit ID 和完成状态。

### SQLT-3B4c 开发方案（2026-08-05）

1. 新增 `SQLT-DQL-085` 作为 1 MiB 单字段边界，使用双方言一致的确定性 `REPEAT` 表达式；
   新增 `SQLT-DQL-086` 作为 10,000 行有序结果集边界，使用 4 组十进制 digit relation 的
   `CROSS JOIN` 生成 `1`～`10000`，避免依赖双方言不同的递归深度配置。
2. 两条 case 均使用独立 SQL 文件和四行注释，注册独立 `dql.boundary` capability。普通
   `dql-oracles.json`、锁 `dql-lock-oracles.json` 和新边界 oracle 按 capability 明确分流，
   validator 必须拒绝漏 case、漏 dialect、未知字段和非法摘要值。
3. 新增流式摘要工具，以固定 64 KiB chunk 读取客户端输出，只累计 SHA-256、总字节数、行数、
   最大行字节数和末尾 LF 状态，不把 1 MiB 行或 10,000 行结果整体读入内存；增加单元测试覆盖
   chunk 边界、超长单行、空输出和缺失末尾 LF。
4. 新增专用 boundary runner，将 SQL 客户端 stdout 直接写入
   `/Volumes/fushilu/.caches/data-nexus/` 下的原始文件，再流式计算摘要并与版本化 oracle 精确比较。
   固定 `mysql:8.0.42`、`postgres:16.8`，同时执行 backend direct 与 security-off gateway，
   默认完整验收为 `2 case × 2 dialect × 2 path = 8` 次。
5. 聚焦调试可以使用 case/source 过滤，但最终验收必须不设置过滤；完成后重跑普通 DQL 141/141、
   锁 DQL 16/16、边界 DQL 8/8，并独立提交 SQLT-3B4c。随后用纯文档提交记录真实 commit ID、
   更新 `todo-impl.md` 并关闭 SQLT-3B4。

- [x] 增加 PostgreSQL `ILIKE`。
- [x] 增加 PostgreSQL `NULLS FIRST`。
- [x] 增加 PostgreSQL `NULLS LAST`。
- [x] 增加 MySQL 等价 NULL 排序表达式，并单独记录方言语义。
- [x] 增加 qualified wildcard `t.*`。
- [x] 增加 alias wildcard。
- [x] 增加多层 alias 遮蔽。
- [x] 增加重复列名和列授权边界。
- [x] 增加 PostgreSQL array 构造、索引或包含查询。
- [x] 增加 JSON 嵌套路径和 JSON 数组查询。
- [x] 增加日期 interval 运算。
- [x] 增加 timestamp 范围过滤。
- [x] 增加 `FOR UPDATE`。
- [x] 增加 `FOR SHARE`。
- [x] 增加 `NOWAIT`。
- [x] 增加 `SKIP LOCKED`。
- [x] 使用两个真实连接验证锁等待、NOWAIT 错误和 SKIP LOCKED 返回集合；默认完整 Docker
  锁回归 direct/security-off gateway 16/16 通过。
- [x] 增加 SQLT-3B4c 1 MiB 长字段和 10,000 行结果集边界；runner 使用 64 KiB chunk，
  direct/security-off gateway 默认完整 Docker 回归 8/8 通过。
- [x] 将 DQL canonical corpus 补到至少 80 条。
- [x] 更新完整 DQL oracle 并执行无范围过滤的完整 Docker 回归（141/141）。
- [x] 独立提交 `SQLT-3B4`：`e73fde0`、`57e1c67`、`8ab72e7`。

## 7. SQLT-3D：DDL corpus

### 7.1 SQLT-3D1：Table 生命周期

#### 开发方案（2026-08-05）

- [x] **SQLT-3D1a：建表语义**（`1bbf74b`）。保留 `SQLT-DDL-001` 生命周期 smoke，新增独立的普通
  `CREATE TABLE`、`CREATE TABLE IF NOT EXISTS` 和重复 CREATE 稳定错误 case；每个 case
  使用独立 SQL 文件，双方言共用语法时仍分别验证 MySQL 8.0.42 和 PostgreSQL 16.8。
- [x] **SQLT-3D1b：列演进与重命名**（`9661dee`）。新增 ADD COLUMN、DROP COLUMN、RENAME COLUMN、列类型
  变更和 table rename；列类型变更按方言拆分 SQL，避免把方言差异隐藏在 runner 中。
  - [x] `SQLT-DDL-005`：双方言 ADD COLUMN，精确验证新增列序、类型和可空性。
  - [x] `SQLT-DDL-006`：双方言 DROP COLUMN，精确验证目标列消失且其余列序稳定。
  - [x] `SQLT-DDL-007`：双方言 RENAME COLUMN，验证旧列名消失、新列名保留原类型。
  - [x] `SQLT-DDL-008`：MySQL `MODIFY COLUMN` 将 `VARCHAR(16)` 扩为 `VARCHAR(80)`。
  - [x] `SQLT-DDL-009`：PostgreSQL `ALTER COLUMN TYPE` 完成等价类型扩容。
  - [x] `SQLT-DDL-010`：双方言 table rename，验证旧对象消失、新对象及列定义完整保留。
  - [x] 复用按 case 重建的 basic/with-note setup fixture；不在 case SQL 中混入前置建表语句。
  - [x] PostgreSQL catalog 类型快照保留 `varchar(n)` 长度，并同步既有 DDL oracle。
  - [x] 默认 DDL runner 范围扩至 `001`～`010`，完成 36 次 direct/gateway 全回归。
- [x] **SQLT-3D1c：删除、清空和临时表**（`79c1f39`、`6123c7f`）。新增 DROP、DROP IF EXISTS、TRUNCATE，以及同一连接
  可见、另一连接不可见、断开连接后消失的临时表会话生命周期。
  - [x] **SQLT-3D1c1：普通对象删除与清空**（`79c1f39`）。
  - [x] `SQLT-DDL-011`：双方言 DROP TABLE，catalog 快照必须为空。
  - [x] `SQLT-DDL-012`：双方言 DROP TABLE IF EXISTS 作用于不存在对象，执行成功且 catalog 不变。
  - [x] `SQLT-DDL-013`：双方言 TRUNCATE，catalog 定义保持不变且数据探针从 3 行精确变为 0 行。
  - [x] 扩展 `ddl-oracles.json` 支持可选 `data_query`、`before_data` 和 `after_data`；validator
    要求三者成组出现、SQL 路径受限，并由普通 DDL runner 精确比较前后数据状态。
  - [x] D1c1 完成后默认普通 DDL 回归为 `001`～`013`，共 48 次 `case x dialect x path`。
  - [x] **SQLT-3D1c2：临时表会话生命周期**（`6123c7f`）。
  - [x] `SQLT-DDL-014` 使用双方言 `CREATE TEMPORARY TABLE` 兼容语法，SQL 文件只包含建表语句。
  - [x] 新建临时表 oracle/runner；同一连接内创建、插入和读取，第二连接查询必须返回稳定
    “对象不存在”错误，创建连接断开后新连接查询也必须返回相同错误。
  - [x] 临时表 direct 和 security-off gateway 分别验收；gateway 的两个客户端连接不得复用同一
    backend session，需以隔离结果和断线回收结果共同证明会话边界。
  - [x] 临时对象 raw/error/summary 继续外置，runner 必须显式关闭所有客户端进程并清理容器。
  - [x] D1c1 与 D1c2 分别提交，最后完成普通 DDL 与临时表完整回归后再关闭 SQLT-3D1。
- [x] 新建 `ddl-oracles.json`，仅覆盖 canonical DDL case；每个 case/dialect 明确声明
  `result`、执行前准备 SQL、catalog snapshot 查询、精确执行后状态，以及失败时稳定错误身份。
- [x] 新建 `run-ddl-corpus.sh`；每个 `case x dialect x path` 均先通过 direct backend 重建基线，
  再分别执行 direct 和 security-off gateway，禁止前一 case 的 schema 状态泄漏到后一 case。
- [x] DDL 成功验收必须比较精确 catalog snapshot，而不是仅比较客户端 command tag；失败验收必须
  同时比较 MySQL error number/SQLSTATE 或 PostgreSQL SQLSTATE，并证明 catalog 前后无变化。
- [x] catalog snapshot 统一输出稳定 TSV：table、column ordinal/name/type/nullability/default；查询必须
  限定 `sqlt_ddl_%` 测试对象并显式排序，避免数据库内部对象和展示格式造成不稳定结果。
- [x] validator 强制 oracle 与 manifest DDL case/dialect 完整覆盖，拒绝未知 case、跨目录 SQL、缺失
  状态/错误字段和非法执行模式；新增负向单元测试固定该契约。
- [x] 所有 raw、normalized、diff、summary、Docker 和构建产物继续写入
  `/Volumes/fushilu/.caches/data-nexus/`；仓库内不生成测试输出。
- [x] 每个切片依次执行 manifest validator、Python unittest、固定版本 Docker direct/gateway 完整
  DDL 回归；功能提交后再用独立文档提交回填真实 commit ID。

- [x] 增加 `CREATE TABLE`：`SQLT-DDL-002`。
- [x] 增加 `CREATE TABLE IF NOT EXISTS`：`SQLT-DDL-003`。
- [x] 增加重复 CREATE 错误：`SQLT-DDL-004`。
- [x] 增加 ALTER ADD COLUMN：`SQLT-DDL-005`。
- [x] 增加 ALTER DROP COLUMN：`SQLT-DDL-006`。
- [x] 增加 ALTER RENAME COLUMN：`SQLT-DDL-007`。
- [x] 增加 ALTER COLUMN 类型或方言等价写法：`SQLT-DDL-008`、`SQLT-DDL-009`。
- [x] 增加 RENAME TABLE：`SQLT-DDL-010`。
- [x] 增加 DROP TABLE：`SQLT-DDL-011`。
- [x] 增加 DROP TABLE IF EXISTS：`SQLT-DDL-012`。
- [x] 增加 TRUNCATE：`SQLT-DDL-013`。
- [x] 增加临时表及会话生命周期：`SQLT-DDL-014`。
- [x] 每条 DDL 检查执行后的 catalog/object 状态。
- [x] 独立提交 `SQLT-3D1`：`1bbf74b`、`9661dee`、`79c1f39`、`6123c7f`。

### 7.2 SQLT-3D2：约束与索引

#### 开发方案（2026-08-05）

- [x] **SQLT-3D2a：结构约束生命周期**。
- [x] `SQLT-DDL-015`：双方言为无主键表增加命名 PRIMARY KEY，并验证约束类型与列序。
- [x] `SQLT-DDL-016`、`017`：分别使用 MySQL `DROP PRIMARY KEY` 和 PostgreSQL
  `DROP CONSTRAINT` 删除主键，验证约束消失但列定义保留。
- [x] `SQLT-DDL-018`：双方言增加命名 UNIQUE 约束，并验证唯一约束及后端隐式索引身份。
- [x] `SQLT-DDL-019`：双方言增加命名 FOREIGN KEY，验证源列、目标表/列和约束类型。
- [x] `SQLT-DDL-020`：双方言增加命名 CHECK，验证约束存在并执行一条违反条件的写入错误探针。
- [x] 新增可选 metadata probe 契约，精确输出 constraint/index kind、对象名、列序、引用目标和
  unique 标志；字段成组校验，SQL 路径必须位于 matrix 根目录。
- [x] D2a 默认普通 DDL 回归扩至 `001`～`020`，排除专用临时表 runner 管理的 `014` 后为
  68 次 `case x dialect x path`；`014` 多连接矩阵另为 4 次，D2a 合计 72 次。
- [x] **SQLT-3D2b：列约束与冲突无副作用**。
- [x] 增加 DEFAULT 与 NOT NULL 生命周期，语法不兼容时按方言拆分 SQL 文件。
- [x] 增加已有重复值上的 UNIQUE、孤儿行上的 FOREIGN KEY、已有非法值上的 CHECK/NOT NULL
  失败 case；固定稳定错误身份，并比较 catalog/data 前后完全一致。

#### SQLT-3D2b 开发方案（2026-08-05）

- [x] 将 D2b 拆成两个独立功能提交：D2b1 完成 DEFAULT/NOT NULL 正向生命周期，D2b2 完成
  脏数据冲突与 catalog/data 无副作用；每个功能提交后均以独立文档提交回填真实 commit ID。
- [x] `SQLT-DDL-021`、`022` 使用双方言兼容的 `ALTER COLUMN ... SET/DROP DEFAULT`，分别验证
  default metadata 从空变为整数常量、再从预置常量恢复为空，列类型和 nullable 状态不漂移。
- [x] `SQLT-DDL-023`、`024` 按方言使用 MySQL `MODIFY COLUMN ... NOT NULL` 与 PostgreSQL
  `ALTER COLUMN ... SET NOT NULL`；fixture 只含非 NULL 行，验证 nullable 从 YES 变为 NO 且数据不变。
- [x] `SQLT-DDL-025`、`026` 按方言使用 MySQL `MODIFY COLUMN ... NULL` 与 PostgreSQL
  `ALTER COLUMN ... DROP NOT NULL`；验证 nullable 从 NO 变为 YES，列类型/default/数据不漂移。
- [x] D2b1 新增 6 个 case、8 个 case/dialect 组合；聚焦 Docker 为 16 次，普通 DDL `001`～
  `026` 完整回归为 84 次，临时表多连接回归另为 4 次。
- [x] D2b2 为已有重复值 UNIQUE、孤儿行 FOREIGN KEY、已有非法值 CHECK/NOT NULL 各建独立
  SQL case 与 setup；固定双方言错误 identity，并要求 `unchanged=true`、数据 probe 前后逐字一致。
- [x] 所有新增 case/setup/oracle 使用独立 SQL 文件和四行说明注释；完成 validator、Python
  unittest、全部 shell `bash -n`、`git diff --check`，产物继续写入外部 cache。

#### SQLT-3D2b1 验收（2026-08-05）

- [x] 功能提交：`fc6d69a`（`test(SQLT-3D2b1): verify default and nullability lifecycle`）。
- [x] `SQLT-DDL-021`～`026` 覆盖 DEFAULT 设置/删除和双方言 NOT NULL 设置/删除；每条路径
  精确验证 column type、nullable、default，并证明两行原始数据逐字不变。
- [x] 聚焦 Docker 16/16、普通 DDL 完整回归 84/84、临时表多连接回归 4/4：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2b1-focused-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2b1-full-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2b1-temp-regression-20260805/`。
- [x] validator 180 cases、Python unittest 64/64、全部 shell `bash -n` 与
  `git diff --check` 通过；无非空 diff 或残留 Docker 容器。

#### SQLT-3D2b2 开发方案（2026-08-05）

- [x] `SQLT-DDL-027` 在已有重复 `probe_value` 上增加命名 UNIQUE；预期 MySQL
  `1062/23000`、PostgreSQL `23505`，约束/隐式索引不得残留。
- [x] `SQLT-DDL-028` 在已有孤儿行上增加命名 FOREIGN KEY；预期 MySQL `1452/23000`、
  PostgreSQL `23503`，父子表数据与 catalog 必须不变。
- [x] `SQLT-DDL-029` 在已有非正数上增加命名 CHECK；预期 MySQL `3819/HY000`、PostgreSQL
  `23514`，失败后不得残留 CHECK metadata。
- [x] `SQLT-DDL-030`、`031` 按方言在已有 NULL 的列上执行 MySQL `MODIFY ... NOT NULL` 和
  PostgreSQL `SET NOT NULL`；使用真实 Docker 校准 MySQL `1138/22004`、PostgreSQL `23502`。
- [x] 允许失败 DDL oracle 同时声明 data probe，但仍强制 `unchanged=true`；新增 validator 单测，
  每个冲突 case 均比较 catalog before/after、精确稳定错误和业务数据 before/after。
- [x] D2b2 新增 5 个 case、8 个 case/dialect 组合；聚焦 Docker 16 次，普通 DDL `001`～
  `031` 完整回归 100 次，临时表多连接回归另为 4 次。
- [x] 完成 validator、Python unittest、全部 shell `bash -n` 和 `git diff --check`；功能与真实
  验收回填继续使用两个独立 commit，所有产物写入外部 cache。
- [x] **SQLT-3D2c：索引生命周期**。
- [x] 增加普通索引、唯一索引、双方言 DROP INDEX 和重复索引名错误。
- [x] 索引 oracle 必须验证名称、列序、唯一性和删除后的对象状态；重复名称错误必须无 schema
  副作用。
- [x] D2a、D2b、D2c 分别完成 validator、Python unittest、固定版本 Docker direct/gateway
  完整回归并独立提交，最后再关闭 SQLT-3D2。

#### SQLT-3D2c 开发方案（2026-08-05）

- [x] `SQLT-DDL-032` 使用双方言通用 `CREATE INDEX` 创建两列普通索引；索引 oracle 精确验证
  `sqlt_idx_probe` 名称、`probe_name, probe_id` 列序和 `is_unique=NO`。
- [x] `SQLT-DDL-033` 使用双方言通用 `CREATE UNIQUE INDEX` 创建两列唯一索引；fixture 预置
  无重复组合，oracle 精确验证列序和 `is_unique=YES`，并与 UNIQUE 约束隐式索引区分。
- [x] `SQLT-DDL-034`、`035` 分别使用 MySQL `DROP INDEX ... ON table` 和 PostgreSQL
  `DROP INDEX ...`；setup 预建命名普通索引，执行后索引快照必须为空且表列 catalog 不漂移。
- [x] `SQLT-DDL-036` 在 setup 已存在 `sqlt_idx_probe` 时再次 `CREATE INDEX`；预期 MySQL
  `1061/42000`、PostgreSQL `42P07`，要求 `unchanged=true` 且索引快照前后逐字一致。
- [x] 新增双方言 `sqlt_ddl_index` fixture、稳定索引 metadata query，并将该表纳入 cleanup；每个
  case、setup 和 oracle 均使用独立 SQL 文件及四行说明注释，SQL 文本不嵌入 JSON/runner。
- [x] D2c 新增 5 个 case、8 个 case/dialect 组合；先执行 `032`～`036` 聚焦 Docker 16 次，
  再执行普通 DDL `001`～`036` 完整回归 116 次，并复跑临时表多连接 4 次。
- [x] 完成 validator、Python unittest、全部 shell `bash -n` 和 `git diff --check`；功能提交后
  以独立文档提交回填真实 commit ID、Docker 证据目录及 manifest 190/DDL 36 的新基线。

#### SQLT-3D2c 验收（2026-08-05）

- [x] 功能提交：`4fed1c6`（`test(SQLT-3D2c): verify index lifecycle`）。
- [x] `SQLT-DDL-032`～`036` 覆盖普通/唯一复合索引、双方言 DROP INDEX 与重复索引名；
  direct/gateway 均精确验证 `probe_name, probe_id` 列序和 unique 标志。
- [x] 重复名称错误固定为 MySQL `1061/42000`、PostgreSQL `42P07`，失败后原索引 metadata
  与表列 catalog 均逐字不变。
- [x] 聚焦 Docker 16/16、普通 DDL 完整回归 116/116、临时表多连接回归 4/4：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2c-focused-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2c-full-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2c-temp-regression-20260805/`。
- [x] validator 190 cases、Python unittest 65/65、全部 shell `bash -n` 与
  `git diff --check` 通过；无非空 diff。

#### SQLT-3D2b2 验收（2026-08-05）

- [x] 功能提交：`7720c55`（`test(SQLT-3D2b2): verify constraint conflict rollback`）。
- [x] `SQLT-DDL-027`～`031` 固定双方言 UNIQUE、FOREIGN KEY、CHECK、NOT NULL 脏数据冲突
  identity；失败后 catalog before/after、约束 metadata 和原始数据逐字一致。
- [x] 聚焦 Docker 16/16、普通 DDL 完整回归 100/100、临时表多连接回归 4/4：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2b2-focused-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2b2-full-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2b2-temp-regression-20260805/`。
- [x] validator 185 cases、Python unittest 65/65、全部 shell `bash -n` 与
  `git diff --check` 通过；无非空 diff。

#### SQLT-3D2a1 验收（2026-08-05）

- [x] 功能提交：`7eaafbe`（`test(SQLT-3D2a1): verify primary key lifecycle`）。
- [x] `SQLT-DDL-015` 在双方言增加主键；`016`、`017` 按方言删除主键，列定义保持不变。
- [x] MySQL metadata 精确为 `PRIMARY / PRIMARY KEY / probe_id`；PostgreSQL 精确为
  `sqlt_pk_probe / PRIMARY KEY / probe_id`，删除后均为空。
- [x] 本切片的约束 metadata probe 覆盖对象名、约束类型、列序和引用目标；索引 kind 与
  unique 标志留到 `SQLT-3D2c` 补齐。
- [x] 普通 DDL runner 仅选择 `ddl-oracles.json` 覆盖的 case，跳过由多连接 runner 管理的
  `SQLT-DDL-014`；新增 2 个筛选单测。
- [x] 固定 MySQL 8.0.42/PostgreSQL 16.8 direct 与 security-off gateway 完整回归 56/56：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2a1-full-rerun-20260805/`。
- [x] validator 171 cases、Python unittest 61/61、全部 shell `bash -n` 与
  `git diff --check` 通过；Docker compose 无残留容器。

#### SQLT-3D2a2 开发方案（2026-08-05）

- [ ] `SQLT-DDL-018` 使用双方言通用的 `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE`，fixture
  预置无约束且无重复值的数据；oracle 同时验证约束名/类型/列序，以及后端隐式唯一索引的
  名称、列序和 unique 标志。
- [ ] `SQLT-DDL-019` 预置带主键的引用表和无外键的子表，再增加命名 FOREIGN KEY；oracle
  精确验证源列、引用表、引用列和约束类型，cleanup 按子表到父表顺序清理以避免残留。
- [ ] `SQLT-DDL-020` 先成功增加命名 CHECK，再由 runner 沿当前 direct/gateway 路径执行独立的
  违反条件写入 SQL；MySQL 固定错误号/SQLSTATE，PostgreSQL 固定 SQLSTATE，并验证失败写入
  后行数仍为零、CHECK metadata 和 catalog 均保持正确。
- [ ] 扩展普通 DDL oracle 的可选 `error_probe`/`probe_error` 成组契约；validator 要求探针 SQL
  位于 matrix 根目录、预期错误以 LF 结尾，并增加缺字段与路径越界负向单测。
- [ ] 每个 case、setup、metadata oracle 和负向探针均使用独立 SQL 文件并保留四行说明注释；
  不在 manifest 或 runner 中嵌入 SQL 文本。
- [ ] 先运行 `018`～`020` 聚焦 Docker，再运行普通 DDL `001`～`020` 的 68 次完整回归；复跑
  `014` 多连接 4 次回归，最后执行 validator、Python unittest、全部 shell `bash -n` 和
  `git diff --check`。
- [x] 功能实现使用独立 `SQLT-3D2a2` commit；验收通过后以另一文档 commit 回填真实 commit
  ID、Docker 产物目录、精确执行数和更新后的 manifest/最低预算基线。

#### SQLT-3D2a2 验收（2026-08-05）

- [x] 功能提交：`9190f1b`（`test(SQLT-3D2a2): verify unique foreign and check constraints`）。
- [x] `SQLT-DDL-018` 在双方言精确验证 `sqlt_uq_probe` UNIQUE 约束及同名隐式唯一索引的
  对象类型、列序、列名和 unique 标志。
- [x] `SQLT-DDL-019` 在双方言精确验证 `parent_id -> sqlt_ddl_parent.parent_id` 引用映射。
- [x] `SQLT-DDL-020` CHECK DDL 成功后，direct/gateway 非法 INSERT 均失败；MySQL 为
  `3819/HY000`，PostgreSQL 为 `23514`，失败后受保护表仍为 0 行。
- [x] 聚焦 Docker 12/12、普通 DDL 完整回归 68/68、临时表多连接回归 4/4：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2a2-focused-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2a2-full-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d2a2-temp-regression-20260805/`。
- [x] validator 174 cases、Python unittest 64/64、全部 shell `bash -n` 与
  `git diff --check` 通过；无非空 diff 或残留 Docker 容器。

- [x] 增加 PRIMARY KEY 生命周期。
- [x] 增加 UNIQUE 约束。
- [x] 增加 FOREIGN KEY 约束。
- [x] 增加 CHECK 约束。
- [x] 增加 DEFAULT 约束。
- [x] 增加 NOT NULL 约束。
- [x] 增加 CREATE INDEX。
- [x] 增加 UNIQUE INDEX。
- [x] 增加 DROP INDEX，并按方言拆分语法。
- [x] 增加重复索引名错误。
- [x] 增加约束冲突并证明没有残留 schema 副作用。
- [x] 独立提交 `SQLT-3D2`：`7eaafbe`、`9190f1b`、`fc6d69a`、`7720c55`、`4fed1c6`。

### 7.3 SQLT-3D3：其他对象和事务差异

- [x] 增加 CREATE/DROP VIEW。
- [x] 增加 PostgreSQL schema 生命周期。
- [x] 增加 PostgreSQL sequence 生命周期。
- [ ] 增加 MySQL database 能力边界。
- [ ] 增加 COMMENT 能力与方言差异。
- [ ] 增加 PostgreSQL 事务内 DDL rollback。
- [ ] 增加 MySQL DDL 隐式提交差异。
- [ ] 增加 DDL allow、ticket 和 deny。
- [ ] 增加权限不足错误。
- [x] 增加对象不存在错误。
- [ ] 增加并发 DDL 锁冲突。
- [x] 将 DDL canonical corpus 补到至少 30 条。
- [ ] 固化 catalog 状态、错误身份、事务状态和无副作用 oracle。
- [ ] 独立提交 `SQLT-3D3`。

#### SQLT-3D3a 开发方案（2026-08-05）

- [x] `SQLT-DDL-037` 使用双方言通用 `CREATE VIEW`，从 fixture 基础表投影并重命名两列；
  oracle 精确验证对象类型为 VIEW、列序/列名/类型，以及基础表列 catalog 不漂移。
- [x] `SQLT-DDL-038` 使用双方言通用 `DROP VIEW` 删除预建视图；执行后 VIEW metadata 必须为空，
  基础表及其列定义保持不变。
- [x] `SQLT-DDL-039` 对已存在的同名 VIEW 再次 CREATE；固定 MySQL `1050/42S01`、PostgreSQL
  `42P07`，要求 `unchanged=true` 且对象/列 metadata 前后逐字一致。
- [x] `SQLT-DDL-040` 创建引用不存在基础表的 VIEW；先按真实固定版本 Docker 校准 MySQL 与
  PostgreSQL 错误 identity，再证明失败后不产生 VIEW 对象或列 metadata。
- [x] `SQLT-DDL-041` 删除不存在的 VIEW；校准双方言稳定错误 identity，并验证空 catalog 和空
  VIEW metadata 不变，为后续对象不存在错误 corpus 提供 canonical 基线。
- [x] 新增 `ddl.views` capability、双方言 VIEW setup/metadata oracle，并在 cleanup 中先 DROP VIEW
  再 DROP 基础表；每个 case、setup、oracle 使用独立 SQL 文件和四行说明注释。
- [x] D3a 新增 5 个双方言 case；先执行 `037`～`041` 聚焦 Docker 20 次，再执行普通 DDL
  `001`～`041` 完整回归 136 次，并复跑临时表多连接 4 次。
- [x] 完成 validator、Python unittest、全部 shell `bash -n` 和 `git diff --check`；功能提交后
  使用独立文档提交回填真实 commit ID、错误 identity、证据目录及 manifest 195/DDL 41 基线。

#### SQLT-3D3a 验收（2026-08-06）

- [x] 功能提交：`906bb83`（`test(SQLT-3D3a): verify view lifecycle`）。
- [x] `SQLT-DDL-037`～`041` 覆盖 CREATE/DROP VIEW、重复创建、缺失 source table 和删除缺失
  VIEW；成功路径精确比较对象类型、投影列序/列名/类型，失败路径证明 metadata 前后无变化。
- [x] 稳定错误 identity：重复 CREATE 为 MySQL `1050/42S01`、PostgreSQL `42P07`；缺失 source
  为 MySQL `1146/42S02`、PostgreSQL `42P01`；缺失 VIEW 为 MySQL `1051/42S02`、PostgreSQL
  `42P01`，direct 与 security-off gateway 完全一致。
- [x] MySQL existing VIEW fixture 使用 `CREATE DEFINER = 'sqlt'@'%' VIEW`，使 direct/gateway
  都验收普通业务账号的 VIEW 生命周期；root definer 所需 `SYSTEM_USER` 权限边界不混入本切片。
- [x] DDL runner 的结构化结果中 `execution` 记录真实 `success/error`，并新增
  `expected_result` 保存 oracle 预期，避免错误 case 被错误记录为成功执行。
- [x] 聚焦 Docker 20/20、普通 DDL 完整回归 136/136、临时表多连接回归 4/4：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3a-focused-rerun-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3a-full-20260805/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3a-temp-regression-20260806/`。
- [x] validator 195 cases、Python unittest 65/65、全部 shell `bash -n` 与
  `git diff --check` 通过；三组回归均无非空 diff。

#### SQLT-3D3b 开发方案（2026-08-06）

- [x] `SQLT-DDL-042` 使用 PostgreSQL `CREATE SCHEMA sqlt_ddl_schema`；namespace oracle 精确
  验证 schema 名称、owner 和空对象集合，既有 `public` 测试对象 catalog 不漂移。
- [x] `SQLT-DDL-043` 使用不带 `CASCADE` 的 `DROP SCHEMA sqlt_ddl_schema` 删除预建空 schema；
  执行后 namespace/object probe 必须为空，避免把级联删除语义混入基础生命周期。
- [x] `SQLT-DDL-044` 对已存在 schema 重复 CREATE；使用 PostgreSQL 16.8 Docker 校准并固定
  `duplicate_schema` 错误 identity，要求 namespace/object probe 和通用 catalog 前后逐字一致。
- [x] `SQLT-DDL-045` 删除包含一张 probe table 的非空 schema；固定依赖对象错误 identity，并
  证明失败后 schema、owner、内部表和列 metadata 全部保留，不使用 `CASCADE` 弱化断言。
- [x] `SQLT-DDL-046` 删除不存在 schema；固定 `invalid_schema_name` 错误 identity，并验证空
  namespace/object probe 与通用 catalog 均无副作用。
- [x] 新增 PostgreSQL-only `ddl.schemas` capability、empty/nonempty schema setup 和稳定
  namespace/object oracle；case、fixture、oracle 均使用独立 SQL 文件与四行说明注释。MySQL
  database 能力边界留给独立切片，不伪装成跨方言 schema case。
- [x] cleanup 在删除普通测试表后执行 `DROP SCHEMA IF EXISTS sqlt_ddl_schema CASCADE`，仅用于
  case 隔离；case 本身不使用 `CASCADE`。validator 继续要求 manifest 与 DDL oracle 方言闭合。
- [x] D3b 新增 5 个 PostgreSQL-only case；先执行 `042`～`046` 聚焦 Docker 10 次，再执行普通
  DDL `001`～`046` 完整回归 146 次，并复跑临时表多连接 4 次。
- [x] 完成 validator、Python unittest、全部 shell `bash -n` 和 `git diff --check`；功能提交后
  使用独立文档提交回填真实 commit ID、错误 identity、证据目录及 manifest 200/DDL 46 基线。

#### SQLT-3D3b 验收（2026-08-06）

- [x] 功能提交：`3426b19`（`test(SQLT-3D3b): verify schema lifecycle`）。
- [x] `SQLT-DDL-042`～`046` 覆盖 CREATE/DROP 空 schema、重复创建、删除非空 schema 和删除
  缺失 schema；namespace probe 精确比较 owner、对象类型/名称、列序/类型/可空性。
- [x] 稳定错误 identity：重复 CREATE 为 PostgreSQL `42P06`，非空 DROP 为 `2BP01`，缺失
  schema DROP 为 `3F000`；direct 与 security-off gateway 完全一致。
- [x] 非空 DROP 失败后 `sqlt_ddl_schema.sqlt_ddl_probe` 及两列 metadata 前后逐字一致；case SQL
  不使用 `CASCADE`，仅 cleanup 使用 `CASCADE` 隔离下一 case。
- [x] 聚焦 Docker 10/10、普通 DDL 完整回归 146/146、临时表多连接回归 4/4：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3b-focused-20260806/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3b-full-20260806/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3b-temp-regression-20260806/`。
- [x] validator 200 cases、Python unittest 65/65、全部 shell `bash -n` 与
  `git diff --check` 通过；三组回归均无非空 diff。

#### SQLT-3D3c 开发方案（2026-08-06）

- [x] `SQLT-DDL-047` 使用 PostgreSQL `CREATE SEQUENCE` 创建显式 `START/MINVALUE/MAXVALUE/
  INCREMENT/CACHE/NO CYCLE` 参数的 sequence；oracle 精确验证 schema、owner、数据类型、全部
  参数和尚未取值的运行状态。
- [x] `SQLT-DDL-048` 对 fixture 已创建并执行一次 `nextval` 的 sequence 使用 `ALTER SEQUENCE`；
  同时修改 increment、max、cache、cycle 并 `RESTART WITH`，验证静态参数与下一次取值基线。
  PostgreSQL `CACHE 3` 首次取值返回 10，但 `pg_sequences.last_value` 持久化首个缓存块上界 20；
  oracle 必须固定该真实语义，不把 fixture 的探针查询登记成独立 DDL case。
- [x] `SQLT-DDL-049` 删除预建 sequence；执行后 sequence metadata 与运行状态 probe 必须为空，
  通用 table catalog 保持不变。
- [x] `SQLT-DDL-050` 对已存在 sequence 重复 CREATE；使用 PostgreSQL 16.8 Docker 校准稳定错误
  identity，并证明 owner、参数和运行状态前后逐字一致。
- [x] `SQLT-DDL-051` ALTER 不存在 sequence，`SQLT-DDL-052` DROP 不存在 sequence；分别校准
  PostgreSQL 错误 identity，要求空 metadata/runtime probe 与通用 catalog 无副作用。
- [x] 新增 PostgreSQL-only `ddl.sequences` capability、basic/called sequence fixture 和稳定
  `pg_sequences` oracle；case、fixture、oracle 均使用独立 SQL 文件与四行说明注释。sequence
  名称固定为 `sqlt_ddl_sequence`，避免扫描系统或业务对象。
- [x] cleanup 在 VIEW/table/schema 清理之后执行 `DROP SEQUENCE IF EXISTS sqlt_ddl_sequence`；
  validator 继续要求 manifest 与 DDL oracle 的 case/dialect 集合严格闭合。
- [x] D3c 新增 6 个 PostgreSQL-only case；先执行 `047`～`052` 聚焦 Docker 12 次，再执行普通
  DDL `001`～`052` 完整回归 158 次，并复跑临时表多连接 4 次。
- [x] 完成 validator、Python unittest、全部 shell `bash -n` 和 `git diff --check`；功能提交后
  使用独立文档提交回填真实 commit ID、错误 identity、证据目录及 manifest 206/DDL 52 基线。

#### SQLT-3D3c 验收（2026-08-06）

- [x] 功能提交：`63884c1`（`test(SQLT-3D3c): verify sequence lifecycle`）。
- [x] `SQLT-DDL-047`～`052` 覆盖 CREATE、参数与 restart 演进、DROP、重复 CREATE、ALTER/DROP
  缺失 sequence；oracle 精确比较 owner、data type、start/min/max/increment/cycle/cache/last value。
- [x] PostgreSQL `CACHE 3` 首次 `nextval` 返回 10，而 `pg_sequences.last_value` 持久化缓存块
  上界 20；ALTER 后 `START=200` 且 `last_value=<NULL>`，证明下一次取值基线重置且尚未调用。
- [x] 稳定错误 identity：重复 CREATE 为 PostgreSQL `42P07`，ALTER 与 DROP 缺失 sequence
  均为 `42P01`；direct 与 security-off gateway 完全一致，失败后参数和运行状态不变。
- [x] 聚焦 Docker 12/12、普通 DDL 完整回归 158/158、临时表多连接回归 4/4：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3c-focused-rerun-20260806/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3c-full-20260806/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3c-temp-regression-20260806/`。
- [x] validator 206 cases、Python unittest 65/65、全部 shell `bash -n` 与
  `git diff --check` 通过；三组通过回归均无非空 diff。

#### SQLT-3D3d 开发方案（2026-08-06）

- [x] `SQLT-DDL-053`、`054` 分别以默认受限 backend 账号执行 `CREATE DATABASE` 与
  `CREATE DATABASE IF NOT EXISTS`；固定 MySQL 8.0.42 权限错误 identity，并由 root catalog
  oracle 证明目标 database 前后均不存在。
- [x] `SQLT-DDL-055`、`056` 分别对 root 预建、但未向 `sqlt` 授权的 database 执行普通与
  `IF EXISTS` DROP；固定权限错误，并证明 database、默认 charset/collation 前后逐字一致。
- [x] `SQLT-DDL-057` 对同一未授权 database 执行 `ALTER DATABASE`；验证权限错误优先于变更，
  且 charset/collation 不漂移。
- [x] 新增 MySQL-only `ddl.database_boundary` capability、existing database fixture、root catalog
  oracle、受限身份/可见 database oracle 和独立 `ddl-database-oracles.json`；所有 SQL 使用独立
  文件和四行说明注释。
- [x] 新建 `run-ddl-database-corpus.sh`：root 只负责健康检查、setup/cleanup 和独立 catalog
  snapshot；direct 通过 `sqlt/sqlt` 连接，gateway 前端仍用静态 `root/root`，但每条路径都必须
  先断言 backend `CURRENT_USER()` 为 `sqlt@%`、当前 database 为 `sqlt`。
- [x] 每条 case 在执行前后同时比较 root 可见的目标 database 状态与受限账号可见 schema 集合；
  gateway 返回的权限错误必须与 direct 一致，拒绝后不得创建、删除或修改 database。
- [x] validator 将 `ddl.database_boundary` 从普通 `ddl-oracles.json` 所有权中排除，并要求专用
  oracle 与 manifest case/dialect 严格闭合；增加正负向 Python 单测和 selector 排除测试。
- [x] D3d 新增 5 个 MySQL-only case，专用权限矩阵共 10 次 direct/gateway；随后回归普通 DDL
  158/158 与临时表 4/4。普通 runner 不使用 root 伪验收这些权限 case。
- [x] 完成 validator、Python unittest、全部 shell `bash -n` 和 `git diff --check`；功能提交后
  使用独立文档提交回填真实 commit ID、错误 identity、证据目录及 manifest 211/DDL 57 基线。

#### SQLT-3D3d 验收（2026-08-06）

- [x] 功能提交：`2c685ca`（`test(SQLT-3D3d): verify database permission boundary`）。
- [x] `SQLT-DDL-053`～`057` 覆盖受限账号 CREATE/DROP/ALTER DATABASE 及
  `IF NOT EXISTS`/`IF EXISTS`；五条语句在 MySQL 8.0.42 direct 与 security-off gateway 均以
  `1044 / 42000` 拒绝。
- [x] root catalog 精确比较 database 名、默认 charset/collation；受限账号可见 database 集合
  在执行前后不变。DROP/ALTER 目标稳定保留 `utf8mb4 / utf8mb4_0900_ai_ci`，CREATE 目标始终
  不存在。
- [x] direct 与 gateway 身份探针均固定 `CURRENT_USER()=sqlt@%`、登录用户名 `sqlt`、
  `DATABASE()=sqlt`；root 只负责健康检查、setup/cleanup 和 catalog 观测，不执行权限 case。
- [x] 专用 Docker 10/10、普通 DDL 完整回归 158/158、临时表多连接回归 4/4：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3d-identity-final-20260806/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3d-full-20260806/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3d-temp-regression-20260806/`。
- [x] validator 211 cases、Python unittest 72/72、全部相关 shell `bash -n` 与
  `git diff --check` 通过；三组通过回归均无失败结果。

## 8. SQLT-3E：TCL、prepared、extended wire 和 cursor

### 8.1 事务控制

#### SQLT-3E1 开发方案（2026-08-06）

- [x] `SQLT-TCL-002` 覆盖单行 `BEGIN`、DML、`COMMIT`；验证 direct/gateway 提交后状态
  相同，连接结束后数据仍可见。
- [x] `SQLT-TCL-003` 覆盖 `BEGIN`、DML、`ROLLBACK`；验证回滚行不存在且连接可继续执行
  一个新的查询/事务。
- [x] `SQLT-TCL-004` 覆盖 `SAVEPOINT`、DML、`RELEASE SAVEPOINT`、`COMMIT`；验证释放
  savepoint 后不能再回滚到该点，最终只保留预期行。
- [x] `SQLT-TCL-005` 覆盖嵌套 savepoint 与两次 `ROLLBACK TO SAVEPOINT`；验证外层 savepoint
  仍可用、内层回滚只撤销内层写入，并固定两方言结果。
- [x] `SQLT-TCL-006` 在显式事务中触发稳定约束错误；验证 PostgreSQL 进入 aborted 状态而
  MySQL 可继续执行，并分别证明 `ROLLBACK`/后续 DML 恢复路径。
- [x] `SQLT-TCL-007` 覆盖双方言 read-only transaction；验证写入被拒绝、事务状态可恢复，
  不把 PostgreSQL 与 MySQL 的错误文本当作跨方言 oracle。
- [x] `SQLT-TCL-008` 设置并读取 MySQL isolation level；使用原生 metadata 查询固定有效值，
  不比较实现无关的默认配置文本。
- [x] `SQLT-TCL-009` 设置并读取 PostgreSQL isolation level；使用原生 metadata 查询固定有效值，
  不比较实现无关的默认配置文本。
- [x] `SQLT-TCL-010` 覆盖事务内 DDL/DML 混合；显式记录 PostgreSQL transactional DDL 与
  MySQL DDL implicit commit 的差异，分别验证 DML/DDL 的最终可见性。
- [x] `SQLT-TCL-011` 覆盖错误后的 `ROLLBACK TO SAVEPOINT`、`RELEASE` 与最终 `COMMIT`，
  作为 savepoint 错误恢复边界，不与 `SQLT-DML-043` 的单连接 DML 恢复 oracle 重复所有权。
- [x] 新增 `tcl.transactions` capability、11 个独立 SQL case、双方言 setup/state/status
  oracle；每个 SQL 文件保留四行注释，SQL 不嵌入 manifest 或 JSON。
- [x] 新建 `tcl-oracles.json` 和 `run-tcl-corpus.sh`；普通 SQL runner 不执行 explicit TCL，
  专用 runner 每条 case 执行 direct/gateway 两条路径，并在 case 后清理连接与 mutation 状态。
- [x] validator 要求 TCL oracle 与 manifest 的 case/dialect 严格闭合，严格限制
  `result`、`state_query`、`state`、`transaction_markers` 及错误 case 的 `error` 字段；已覆盖
  缺字段、额外字段、越界路径、方言漂移和非 TCL case 排除测试。
- [x] 已用 MySQL 8.0.42/PostgreSQL 16.8 Docker 校准错误 identity、事务状态和 DDL implicit
  commit；20 次 case/dialect、40 次 direct/gateway path 全部通过，随后回归 DDL 158/158、
  D3d 10/10 和临时表 4/4。
- [x] 完成 validator、Python unittest `78/78`、全部 shell `bash -n`、`git diff --check`；
  功能提交为 `07a4a5a`（`test(SQLT-3E1): verify transaction lifecycle`）。验收证据：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/tcl-calibration-20260806b/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-tcl-regression-20260806-seq/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-d3d-regression-20260806-seq/`、
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/ddl-temp-regression-20260806/`。

路线调整记录：原方案的 isolation case 拆为 MySQL 专属 `SQLT-TCL-008` 与 PostgreSQL 专属
`SQLT-TCL-009`，事务 DDL 顺延为 `SQLT-TCL-010`，并新增错误后的 savepoint recovery
`SQLT-TCL-011`。因此本阶段基线从原计划的 manifest 220/TCL 10 修正为 manifest 221/TCL 11，
case-dialect 执行数从 18 修正为 20；提交 `df37544` 保留原始方案，以上实现与验收数字以本记录为准。

### 8.2 MySQL prepared

#### SQLT-3E2 开发方案（2026-08-06）

目标：把现有 A10 的 MySQL `COM_STMT_PREPARE`/`COM_STMT_EXECUTE`/`COM_STMT_CLOSE` smoke
提升为可复现的 canonical corpus。后端固定 MySQL 8.0.42，前端只使用真实
`mysql-connector-python` `cursor(prepared=True)`，分别执行 direct backend 与 security-off
gateway；不把普通 text query 的结果冒充 binary prepared 验收。SQL 语句全部保存为独立的
`cases/prepared/*.sql` 文件，保留 `case / Purpose / Expected / Dialect` 四行注释；参数值、
参数类型、列描述、binary row、错误 identity 和最终 mutation 状态写入专用 oracle JSON。

- [x] 先核对当前 MySQL binary decoder/encoder 的能力边界：整数、无符号整数、浮点、
  NULL、decimal、date/time/datetime、bytes 和长字符串；明确 connector 实际发送的 type
  code、unsigned flag、NULL bitmap、列描述与 `COM_STMT_RESET` 行为。若某类型无法通过真实
  wire 稳定往返，先记录为明确 unsupported case，不用 text protocol 绕过。
- [x] 注册 `cursor.mysql_prepared` capability 与 `SQLT-PRP-001`～`008` 八个 case，manifest
  使用 `family=cursor`、`dialects=[mysql]`、`frontends=[mysql_binary]`、
  `protocols=[mysql_binary]`、`statement_mode=single`、`transaction_mode=autocommit`；
  旧 cursor/PostgreSQL extended case 不纳入 prepared 专用 runner。
- [x] `SQLT-PRP-001` 覆盖零参数 `PREPARE`/`EXECUTE`/`CLOSE` 的 SELECT 生命周期；验证
  `COM_STMT_PREPARE` 返回 statement id、列数和列定义，`COM_STMT_EXECUTE` 返回 binary
  resultset，`COM_STMT_CLOSE` 后 statement 不再可执行。
- [x] `SQLT-PRP-002` 覆盖单参数整数与字符串；分别校验一个参数、多个参数、结果行顺序、
  `cursor.description` 列名/数量，以及 direct/gateway 结果严格一致。
- [x] `SQLT-PRP-003` 覆盖 NULL bitmap 与 NULL 参数；验证 NULL 不被变成空字符串、零值或
  文本 `NULL`，并同时包含 NULL predicate 与返回 NULL 列的结果。
- [x] `SQLT-PRP-004` 覆盖 decimal、date、time、datetime/timestamp；参数使用确定性值，
  结果以规范化字符串比较，明确时区、精度和小数位，不依赖本机 locale/timezone。
- [x] `SQLT-PRP-005` 覆盖长字符串与 bytes；使用固定长度边界值，验证 binary length-encoded
  payload、UTF-8 内容和二进制字节不截断、不替换，并把原始响应保存到外部 cache。
- [x] `SQLT-PRP-006` 覆盖参数数量错误、缺失绑定和多余绑定；验证错误在 connector 与
  gateway 均稳定分类，错误后执行 `COM_STMT_RESET`/重新绑定可以恢复，不能留下 mutation。
- [x] `SQLT-PRP-007` 覆盖参数类型边界、重复执行与同一 statement re-bind；同一 prepared
  statement 至少执行三组不同参数（包括 NULL/整数/字符串），验证每次结果独立且无上一轮
  参数残留，最后显式 close。
- [x] `SQLT-PRP-008` 覆盖 prepare 后 schema 改变：先 prepare，再通过独立控制连接增加/改名
  列并执行；分别固定 MySQL direct 与 gateway 的成功、自动重新 prepare 或稳定 backend error
  语义，验证 schema cleanup 后 statement/cache 不泄漏。该 case 不假设跨版本一致的错误文本，
  只比较稳定 errno/SQLSTATE、列描述和最终 catalog。
- [x] 新建 `prepared-oracles.json`、`run-prepared-corpus.sh`、`select_prepared_cases.py`；
  runner 每个 case 先重建 fixture，再执行 direct/gateway 两条真实 binary wire 路径，收集
  prepare metadata、description、rows、status、error identity、mutation/catalog state，
  所有 raw/log/summary 写入 `/Volumes/fushilu/.caches/data-nexus/sql-matrix/<run-id>/`。
- [x] 为 runner 增加 `mysql-connector-python` 固定版本安装/记录机制，优先使用 Docker
  `python:3.12-slim-bookworm`，禁止依赖宿主 Python 包；客户端输出采用 JSON 行协议，避免
  repr、locale 和 warning 污染 oracle。控制连接仅负责 schema setup/cleanup 和 schema-change
  case 的 catalog 观测，不与 prepared client 共享连接。
- [x] 扩展 validator：要求 prepared oracle 与 manifest case/dialect 严格闭合，拒绝未知字段、
  缺参数、参数数量与 SQL placeholder 不一致、越界 SQL/control 路径、把 `mysql_text` 当作
  prepared frontend，以及普通 DML/DQL runner 误选 prepared case；增加 selector/validator
  Python unittest。
- [x] 先执行 decoder/encoder Rust 单测，再用 MySQL 8.0.42 Docker 校准八条 case 的真实协议
  细节；之后执行 `8 case-dialect × 2 path = 16` 次 direct/gateway 验收，并保存每条 case 的
  prepare/execute/close 证据。失败时先修复实现或显式缩小 case 契约，不直接放宽 oracle。
- [x] 完成 `bash -n`、`git diff --check`、validator、全部 Python unittest，回归 SQLT-3E1
  `20/20` case-dialect、DDL `158/158`、D3d `10/10`、临时表 `4/4`；功能与验收文档按
  “方案提交 → 功能提交 → todo 证据提交”三步分别 commit。

#### SQLT-3E2 验收（2026-08-06）

- [x] 方案/corpus 提交：`6eac6bd`、`cb27166`；协议功能提交：`421eeb7`
  （`test(SQLT-3E2): fix MySQL prepared binary lifecycle`）。
- [x] `SQLT-PRP-001`～`008` 使用 `mysql-connector-python==9.4.0` 的
  `cursor(prepared=True)`，覆盖零/多参数、NULL、decimal/temporal、长文本/bytes、参数数量错误、
  重复 re-bind、close 和 schema change 后重新 prepare；SQL 均为带四行注释的独立文件。
- [x] 真实 MySQL 8.0.42 Docker direct 与 security-off gateway 验收 8/8 case-dialect、16/16 path：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/prepared-calibration-20260806-10/`。
- [x] `SQLT-PRP-005` gateway 精确保留 `0001027F80FF`；`SQLT-PRP-008` 在 `2057/HY000`
  `result_rebind_required` 后关闭旧 statement、刷新 backend metadata 并成功读取新增的第二列。
- [x] 回归通过：TCL 20/20 case-dialect、40/40 path，普通 DDL 158/158、D3d 10/10、临时表 4/4；
  证据目录分别为 `tcl-prepared-regression-20260806/`、`ddl-prepared-regression-20260806/`、
  `ddl-database-prepared-regression-20260806/`、`ddl-temp-prepared-regression-20260806/`，均位于
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/`。
- [x] Rust 1.94.1 默认与 `--no-default-features` frontend 聚焦测试均为 20/20；validator
  229 cases、Python unittest 91/91、全部 SQL matrix shell `bash -n`、`git diff --check` 通过，
  Docker SQLT 容器无残留。

路线调整记录：真实 connector 将部分 Python `bytes` 参数标为 STRING wire type，因此 decoder
改为“严格 UTF-8 成功则为文本，否则保留原始 bytes”；`COM_STMT_CLOSE` 按协议不返回 packet；
客户端因 schema change 重建 statement 时，backend describe 路径显式失效同 SQL 的连接级
prepare cache，确保新列 metadata 可见。以上均由真实 Docker 失败校准后修复，未放宽 oracle。

### 8.3 PostgreSQL extended wire

#### SQLT-3E3 开发方案（2026-08-06）

目标：把现有 `smoke-security-stream.sh` 中分散的 PostgreSQL extended query smoke 提升为
可复现的 canonical corpus。后端固定 PostgreSQL 16.8，使用逐帧 raw socket 客户端分别连接
direct backend 与 security-off gateway；客户端完整记录 frontend 消息序列和 backend tag、
ParameterDescription、RowDescription、DataRow、ErrorResponse、CommandComplete、PortalSuspended、
ReadyForQuery，不用 psycopg 的聚合结果替代协议验收。SQL 全部保存为独立的
`cases/extended/*.sql` 文件并保留 `case / Purpose / Expected / Dialect` 四行注释，协议动作和预期
写入专用 oracle JSON；不复制 SQL 正文到 manifest、oracle 或客户端代码。

- [x] 先核对 PostgreSQL 16 extended query flow 和现有 gateway 边界：`Parse`、`Bind`、
  `Describe(S/P)`、`Execute`、`Close(S/P)`、`Flush`、`Sync`；确认一个 extended unit 只在 `Sync`
  后返回 `ReadyForQuery`，错误后忽略非 Sync 消息，以及 named statement/portal、分页余量和事务
  status 是否按连接隔离。发现实现与 PostgreSQL 16.8 direct 行为不一致时先增加 Rust 聚焦测试。
- [x] 注册 `cursor.postgres_extended` capability 与 `SQLT-PGX-001`～`008` 八条 case；manifest
  使用 `family=cursor`、`dialects=[postgres]`、`frontends=[pg_extended]`、
  `protocols=[pg_extended]`、`statement_mode=single`，事务状态 case 使用
  `transaction_mode=explicit`，其余为 `autocommit`；旧普通 DQL/TCL 与 MySQL prepared case 不纳入
  extended 专用 runner。
- [x] `SQLT-PGX-001` 覆盖命名 statement 和单 portal 的完整
  `Parse → Describe(S) → Bind → Describe(P) → Execute → Sync → Close(P/S) → Sync` 生命周期；严格
  校验 ParseComplete、ParameterDescription、两次 RowDescription、BindComplete、DataRow、
  CommandComplete、CloseComplete 和最终 idle `ReadyForQuery` 的次序，Execute 不重复发送描述。
- [x] `SQLT-PGX-002` 覆盖 text Bind 参数的保序改写：整数、字符串、NULL、重复 `$1` 和乱序
  `$2/$1`；校验 placeholder 不被字面量拼接或交换，direct/gateway 的列名、类型 OID、format code
  和结果值一致，并将参数字节只保存在 oracle 中。
- [x] `SQLT-PGX-003` 覆盖同一 named statement 多次 re-Bind 到同名 portal；依次绑定不同值执行，
  验证旧参数、分页 offset、RowDescription suppression 和 result format 不泄漏到新 Bind，最后关闭
  portal 与 statement。
- [x] `SQLT-PGX-004` 覆盖同一 statement 上两个 named portal 并存与交错 Execute；两个 portal
  使用不同参数，验证各自绑定、描述状态与结果互不覆盖，关闭一个 portal 不影响另一个 portal。
- [x] `SQLT-PGX-005` 覆盖 `Execute max_rows=1` 的同 portal 多页读取：前两页必须各返回一个
  DataRow 和 `PortalSuspended`，末页返回 DataRow 与 CommandComplete；只在随后 `Sync` 返回 idle
  ReadyForQuery，并与 direct PostgreSQL 的 message class/分页行序一致。
- [x] `SQLT-PGX-006` 覆盖 statement/portal `Describe` 和 RowDescription：显式 SELECT 列校验列名、
  table OID、attribute number、type OID、type size、type modifier、text format；Bind 请求 binary
  result format 时，portal RowDescription 与 DataRow 按 binary 格式返回，Execute 不发送第三份描述。
- [x] `SQLT-PGX-007` 覆盖 extended unit 中 Execute 错误后的 Sync 恢复：记录稳定 SQLSTATE、severity
  和 message identity，验证错误后追加的 Parse/Bind/Execute 被忽略、`Sync` 返回 idle
  ReadyForQuery，随后新的完整 extended unit 可成功执行且连接无需重建。
- [x] `SQLT-PGX-008` 覆盖 ReadyForQuery 的 `I/T/E` 事务状态：extended `BEGIN` 后为 `T`，事务内
  成功查询保持 `T`，错误后为 `E`，extended `ROLLBACK` 后恢复 `I`；每个 unit 都以 Sync 收口，
  direct/gateway 状态序列必须严格一致。
- [x] 新建 `extended-oracles.json`、`extended_client.py`、`select_extended_cases.py` 和
  `run-extended-corpus.sh`；runner 每条 case 重建 PostgreSQL fixture，执行 direct/gateway 两条真实
  wire 路径，逐条保存 raw frame 摘要、规范化 JSON、stderr、gateway log 和总 summary 到
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/<run-id>/`，并保证 Compose 和 gateway 进程退出清理。
- [x] 客户端实现 PostgreSQL v3 startup、MD5/SASL-SCRAM 认证中 fixture 所需的最小闭合能力，
  gateway 使用静态认证、direct 使用 PostgreSQL 16.8 SCRAM；解析 ErrorResponse、RowDescription、
  DataRow 和 ReadyForQuery 时保留协议字段，不依赖宿主 Python 包。若 direct fixture 需要标准认证
  库，则固定容器镜像和依赖版本并写入证据，禁止自动升级。
- [x] 扩展 validator：要求 extended oracle 与 manifest case 严格闭合，拒绝未知字段、非
  `pg_extended` frontend/protocol、越界 SQL 路径、缺失消息序列、非法 portal/statement 动作、
  placeholder 与参数数量不匹配，以及普通 DQL/TCL/prepared runner 误选 extended case；增加
  selector、oracle schema 和 raw message parser 的 Python unittest。
- [x] 先以 PostgreSQL 16.8 Docker direct 路径校准 8 条 case，再执行
  `8 case-dialect × 2 path = 16` 次 direct/gateway 验收；失败时比较 raw tag 和字段修复 gateway，
  不把 direct/gateway 的协议差异通过宽松 oracle 隐藏。
- [x] 完成协议/frontend/backend Rust 聚焦测试、validator、全部 SQL matrix Python unittest、
  全部 shell `bash -n`、`git diff --check`；回归 SQLT-3E2 prepared 8/8、SQLT-3E1 TCL 20/20、
  DDL 158/158、D3d 10/10、临时表 4/4。功能与验收文档继续按
  “方案提交 → 功能提交 → todo 证据提交”三步分别 commit。

#### SQLT-3E3 验收（2026-08-06）

- [x] 方案提交：`0f7684b`；功能提交：`8f38e85`
  （`test(SQLT-3E3): verify PostgreSQL extended wire lifecycle`）。
- [x] `SQLT-PGX-001`～`008` 使用逐帧 PostgreSQL v3 raw socket 客户端，覆盖完整生命周期、
  参数保序与 NULL、re-Bind、多 portal、`Execute max_rows`/`PortalSuspended`、binary
  RowDescription/DataRow、错误后 Sync 恢复和 ReadyForQuery I/T/E；SQL 均为带四行注释的独立文件。
- [x] PostgreSQL 16.8 Docker direct 与 security-off gateway 验收 8/8 case-dialect、16/16 path：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/extended-acceptance-20260806-03/`，最终 stderr 全为空。
- [x] 回归通过：prepared 8/8 case-dialect、16/16 path，TCL 20/20 case-dialect、40/40 path，
  普通 DDL 158/158、D3d 10/10、临时表 4/4；证据目录分别为
  `prepared-extended-regression-20260806/`、`tcl-extended-regression-20260806-02/`、
  `ddl-extended-regression-20260806/`、`ddl-database-extended-regression-20260806/`、
  `ddl-temp-extended-regression-20260806/`，均位于外部 SQL matrix cache 目录。
- [x] Rust 1.94.1 transport 13/13、PostgreSQL frontend 28/28、backend 31/31；SQLT-3E2 MySQL
  frontend 默认与 `--no-default-features` 均为 20/20；validator 237 cases、Python unittest
  102/102、全部 SQL matrix shell `bash -n`、`git diff --check` 通过，本轮 Docker SQLT 容器无残留。

路线调整记录：PostgreSQL 16.8 direct 在 PGX-007 中可能在 ParseComplete 后立即 ErrorResponse，
gateway 可能先产生 BindComplete，因此 oracle 固定真正的协议不变量“首个 ErrorResponse 后立即进入
ReadyForQuery”，不硬编码错误前的 Complete 数量。完整 TCL 回归还发现
`START TRANSACTION READ ONLY` 的后端 socket 会在 `ReadyForQuery=T` 时被错误放回 idle pool；新增
`SessionReturn::IdleOrTxn`，仅将 `I` 返回 idle pool，将 `T/E` 保留在 transaction slot，且未完整读取
合法 ReadyForQuery 的连接不再复用。该修复保持事务属性和错误状态位于同一后端 session，未放宽 oracle。

### 8.4 Cursor

#### SQLT-3E4 开发方案（2026-08-06）

目标：把现有未被专用 runner 验收的 `SQLT-CURSOR-001` 提升为真实同连接 cursor corpus，并新增
`SQLT-CURSOR-002`～`008` 七条 case。完成后 manifest 预计为 244 条、六大家族 canonical 为
243/230、cursor family 为 24 条，TCL/prepared/cursor 从 28 补到 35/35。后端固定 PostgreSQL
16.8，客户端使用 PostgreSQL v3 simple-query frame 在同一 socket 上逐语句执行；每个 SQL 文件继续
保留 `case / Purpose / Expected / Dialect` 四行注释，并用 `-- @step <name>` 标记协议步骤，禁止把 SQL
正文复制到 oracle 或客户端代码。

- [x] 先校准 PostgreSQL 16.8 的 `DECLARE`、`FETCH`、`CLOSE`、重复 cursor、非 hold/hold COMMIT 和
  backend termination 行为，记录 command tag、列 metadata、行窗口、SQLSTATE、ReadyForQuery 状态与
  socket EOF。核对 gateway 的 process-local `NamedCursorHeld`、事务结束清理和 `Drop` 路径；gateway
  的 `WITH HOLD` 只承诺同一前端 session 跨 COMMIT 可继续 FETCH，不描述为 backend server cursor。
- [x] 注册 cursor 专用 capability/oracle 与 `SQLT-CURSOR-002`～`008`；所有 case 使用
  `family=cursor`、`dialects/backends=[postgres]`、`frontends/protocols=[pg_simple]`、
  `statement_mode=script`，事务型 case 使用 `transaction_mode=explicit`。复验 001，并禁止 DQL、TCL、
  prepared 和 extended runner 误选 cursor 专用 case。
- [x] `SQLT-CURSOR-001` 复验基础生命周期：`BEGIN → DECLARE NO SCROLL → FETCH FORWARD 2 →
  FETCH FORWARD 2 → CLOSE → COMMIT`；校验两个窗口的行序、列 metadata、CommandComplete/空结果边界，
  CLOSE 后同名 FETCH 返回稳定 `34000`，事务最终回到 idle。
- [x] `SQLT-CURSOR-002` 覆盖 `FETCH FORWARD ALL` 与耗尽状态：一次读取全部有序行，后续 FETCH 返回
  零行但 cursor 在 CLOSE 前仍存在；校验 direct/gateway 不重复行、不丢行，也不因 `ALL` 使用无界
  单窗口内存。若实现需要分块 drain，增加 RowStream 聚焦测试证明内部窗口上限。
- [x] `SQLT-CURSOR-003` 覆盖同一 transaction 中两个 named cursor 并存和交错 FETCH；两个 SELECT
  使用不同过滤条件/排序，验证各自 offset、列 metadata 和耗尽状态互不覆盖，关闭一个不影响另一个。
- [x] `SQLT-CURSOR-004` 覆盖重复 cursor 名：第二次 DECLARE 必须返回 PostgreSQL 对应的稳定
  duplicate-cursor SQLSTATE，原 cursor 仍能继续 FETCH；CLOSE 后可在同一 transaction 重新使用该名。
  若 gateway 当前错误码与 direct 不一致，修复语义映射，不在 oracle 中接受任意错误。
- [x] `SQLT-CURSOR-005` 覆盖非 `WITH HOLD` cursor 在 COMMIT 后失效：COMMIT 后 FETCH/CLOSE 返回
  `34000`，随后新 transaction 可用同名重新 DECLARE；同时证明 transaction slot 与 backend
  `idle in transaction` 连接均已释放。
- [x] `SQLT-CURSOR-006` 覆盖 `WITH HOLD` cursor 跨 COMMIT：COMMIT 前后连续 FETCH 保持行序与 offset，
  CLOSE 后失效并可同名重建。direct 路径验证 PostgreSQL server cursor，gateway 路径验证明确标注的
  process-local stream 语义；两条路径比较外部结果契约，不伪造 backend cursor ownership。
- [x] `SQLT-CURSOR-007` 覆盖前端 session 清理：分别发送 protocol `Terminate` 和不发送 Terminate 的
  socket EOF，二者都在 cursor 尚有余量时断开；控制连接轮询确认无遗留 `idle in transaction`，新连接
  可立即用同名 DECLARE/FETCH。增加 `CoreGatewayConnection::Drop`/Quit 聚焦测试与 cleanup 指标断言。
- [x] `SQLT-CURSOR-008` 覆盖 backend 断开：先从 cursor 行中取得 `pg_backend_pid()`，由独立控制连接
  `pg_terminate_backend`，再验证未读 stream 被清理。direct 连接预期 EOF，gateway 前端预期稳定 backend
  error；二者重连后均可同名重建，且无 transaction slot、RowStream 或 PostgreSQL session 残留。
- [x] 新建 `cursor-oracles.json`、`cursor_client.py`、`select_cursor_cases.py` 和
  `run-cursor-corpus.sh`。runner 每条 case 重建 fixture，执行 direct/gateway 两条真实 wire 路径；支持
  多连接、Terminate、裸 EOF、控制连接终止 backend 与 `pg_stat_activity` 轮询。逐步事件、规范化输出、
  stderr、gateway/fixture 日志和 summary 全部写入外部 cache，退出时清理 Compose 与 gateway 进程。
- [x] cursor client 复用 SQLT-3E3 已验证的 startup/SCRAM/message decoder，按 `-- @step` 块读取 SQL，
  每个块发送独立 simple Query frame并读到 ReadyForQuery；不得用分号字符串切割 SQL。将共用 wire helper
  提取为小模块时保持 extended client 行为不变，并为 step parser、DataRow、ErrorResponse、EOF 和
  Terminate 增加 Python unittest。
- [x] 扩展 validator：要求 cursor oracle 与 manifest case/dialect 严格闭合，step 名必须与 SQL 文件
  一一对应，拒绝未知字段、重复 step、非法 frontend/protocol、越界 SQL 路径、未声明的 control action、
  任意错误匹配和缺失 cleanup 断言；增加 selector/validator 负例并保持所有旧 corpus 闭合。
- [x] 先用 PostgreSQL 16.8 Docker direct 校准 8 条 case，再执行 18 次真实 path 验收：7 条常规 case
  各执行 direct/gateway，session cleanup case 额外分别执行 Terminate/EOF。完成 Rust 聚焦测试、validator、全部 Python
  unittest、全部 shell `bash -n`、`git diff --check`，并回归 SQLT-3E3 extended 8/8、SQLT-3E2
  prepared 8/8、SQLT-3E1 TCL 20/20、DDL 158/158、D3d 10/10、临时表 4/4。
- [x] 按“方案提交 → 功能提交 → todo 证据提交”分别 commit；8 条 cursor case、18 条 path 和全部
  回归通过后，才把 SQLT-3E4 与 TCL/prepared/cursor 35/35 标为完成。

#### SQLT-3E4 验收（2026-08-06）

- [x] 方案提交：`fb72a1b`；功能提交：`45cce8f`；实现修复提交：`dc7c8b9`、`dfde63a`、`f14440a`。
- [x] `SQLT-CURSOR-001`～`008` 覆盖 DECLARE/FETCH/CLOSE、FETCH ALL、双游标、重复名 `42P03`、
  COMMIT 清理、WITH HOLD、Terminate/EOF 和 backend termination；SQL 均为带四行注释的独立文件。
- [x] PostgreSQL 16.8 Docker direct 与 security-off gateway 验收 8/8 case-dialect、18/18 path：
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/cursor-acceptance-20260806-04/`。
- [x] 回归通过：extended 8/8、16/16 path，prepared 8/8、16/16 path，TCL 20/20、40/40 path，
  普通 DDL 158/158、D3d 10/10、临时表 4/4；证据目录分别为
  `extended-cursor-regression-20260806/`、`prepared-cursor-regression-20260806/`、
  `tcl-cursor-regression-20260806/`、`ddl-cursor-regression-20260806/`、
  `ddl-database-cursor-regression-20260806/`、`ddl-temp-cursor-regression-20260806/`，均位于外部 cache。
- [x] validator 244 cases、Python unittest 113/113、全部 SQL matrix shell `bash -n`、Rust 1.94.1
  聚焦测试、`cargo check -p runtime_gateway --tests` 和 `git diff --check` 通过，Docker SQLT 容器无残留。

路线调整记录：PostgreSQL direct 的 backend termination 契约为 socket EOF，gateway 将同一故障稳定映射为
SQLSTATE `08006`，重连后均可重建同名 cursor。gateway 的 cursor 是 process-local `RowStream`，不宣称
backend server cursor；`TaggedResultSet` 继续执行 mask、max_rows 和 watermark。direct 校准还确认 session
cleanup 必须分别验收 Terminate 与裸 EOF，因此总路径数由计划的 16 调整为实际 18，未放宽 oracle。

## 9. SQLT-3F：非法、恶意和 unsupported

### SQLT-3F 开发方案（2026-08-06）

目标：在现有 `SQLT-INVALID-001` 与 `SQLT-UNSUPPORTED-001` 基础上新增 28 条 canonical case，把
非法、恶意和 unsupported 家族补到 30/30，并建立可重复的 direct/security-off gateway 错误与零副作用
oracle。manifest 的 `outcome` 继续只使用 `allow/rewrite/deny/unsupported`；专用 oracle 另以
`backend_error`、`gateway_deny`、`gateway_unsupported` 区分错误来源，避免把后端拒绝伪装成策略拒绝。
固定后端为 MySQL 8.0.42 与 PostgreSQL 16.8，所有 SQL 仍为带四行注释的独立文件，证据写入外部 cache。

- [x] **SQLT-3F1 确定性错误（12 条新增）**：覆盖第二种语法错误、不完整 SELECT、不存在的表/列、
  ambiguous column、双方言可稳定复现的类型转换/赋值错误、数值越界、除零与函数参数错误。先用 direct
  Docker 校准双方言 vendor error identity，再验证 security-off gateway 保留等价 MySQL errno/SQLSTATE
  或 PostgreSQL SQLSTATE；每条 case 前后比较固定 probe，证明无 DML/DDL/session 副作用。
- [x] 为 3F1 新建 `invalid-oracles.json`、`select_invalid_cases.py` 与 `run-invalid-corpus.sh`。oracle 按
  `case × dialect × direct/gateway` 固定结果类和精确错误 identity，禁止 `any`、错误文本模糊匹配或只检查
  非零退出码；runner 每个 case 重建 fixture，保存 raw error、规范化 identity、before/after probe、
  gateway log、`results.jsonl` 和 summary，并在退出时清理 Compose/gateway。
- [x] 扩展 validator，要求 invalid oracle 与 manifest 严格闭合、路径不越界、双方言声明一致、每条
  expected error 都有稳定 identity、before/after 必须相等且 `unchanged=true`；增加 selector、normalizer
  与 schema 负例单测。完成 3F1 聚焦 Docker、Rust 错误映射测试和相邻 DQL/DML/TCL 回归后独立提交。
- [x] **SQLT-3F2 协议、词法与资源边界（8 条新增）**：覆盖 MySQL binary prepared 参数数量/类型、
  PostgreSQL extended Bind 参数数量/类型、多语句注入、行/块注释绕过、引号与反斜杠、超长标识符、
  深层嵌套/大 IN 列表、超限 SQL 文本。资源 case 使用显式固定上限，不根据宿主内存动态放大；校验错误后
  同连接恢复、ReadyForQuery/transaction 状态、无部分执行和连接可复用，并作为独立功能提交。
- [x] **SQLT-3F3 危险能力与方言 unsupported（8 条新增）**：覆盖 PostgreSQL `COPY ... PROGRAM`、
  server-side file COPY、`DO`/危险 `CALL`，MySQL `LOAD DATA`、`INTO OUTFILE`，以及 `VACUUM`、
  `ANALYZE`、跨方言语句和受限 system catalog/session 设置。默认采用显式 capability deny/unsupported，
  direct 仅用于校准 vendor 行为且不执行真实程序或写宿主文件；以 canary table/catalog/session probe 证明
  gateway 未把危险语句送达 backend，并作为独立功能提交。
- [x] 3F2/3F3 暴露的 parser 或协议映射缺陷已通过最小 Rust 聚焦测试修复，未放宽 oracle；危险语句
  的拒绝必须在 backend side effect 前发生，日志与产物不得保存攻击 payload 中的敏感值或宿主路径。
- [x] 完整验收达到 30/30 canonical case 全部闭合，validator/Python unittest/shell `bash -n`/Rust 1.94.1
  聚焦测试通过，并回归 DQL、DML、TCL、prepared、extended、cursor 与 DDL 专项；各批次遵循
  “方案提交 → 功能提交 → todo 证据提交”，每个完成的功能单独 commit。

- [x] 增加词法错误和不完整 SELECT。
- [x] 增加不存在的表和列。
- [x] 增加 ambiguous column。
- [x] 增加类型不匹配。
- [x] 增加数值溢出和除零。
- [x] 增加 prepared 参数数量和类型错误。
- [x] 增加多语句注入边界。
- [x] 增加 SQL 注释绕过边界。
- [x] 增加引号和反斜杠边界。
- [x] 增加超长标识符。
- [x] 增加深层嵌套和大 IN 列表。
- [x] 增加超限 SQL 文本。
- [x] 增加 PostgreSQL `COPY ... PROGRAM`。
- [x] 增加 MySQL `LOAD DATA` 和 `INTO OUTFILE`。
- [x] 增加 `VACUUM`、`ANALYZE`、`DO`、`CALL` 能力边界。
- [x] 增加 server-side 文件或程序执行拒绝。
- [x] 增加不支持的方言语句。
- [x] 增加无权限 system catalog 或 session 设置。
- [x] 为每条 case 稳定分类为 deny、unsupported 或 backend error。
- [x] 证明拒绝和 unsupported 不产生 backend 副作用。
- [x] 将非法、恶意和 unsupported canonical corpus 补到至少 30 条。
- [x] 独立提交 `SQLT-3F` 各批次。

### SQLT-3F1 验收记录（2026-08-06）

- [x] 功能提交为 `7f599b7`。manifest 为 256 条，invalid 为 13 条，invalid/unsupported 合计
  14/30；新增 `SQLT-INVALID-002`～`013` 均为带四行注释的独立 SQL 文件。
- [x] 固定 MySQL 8.0.42 与 PostgreSQL 16.8 Docker 验收 44/44 路径通过，其中 MySQL 20/20、
  PostgreSQL 24/24；每条 direct/security-off gateway 路径均精确比对 errno/SQLSTATE，并证明固定
  before/after probe 不变。证据位于外部 cache 的 `invalid-acceptance-20260806-01/`。
- [x] 相邻 DML 64/64、DQL 141/141、TCL 20/20 case 和 40/40 path 回归通过；证据分别位于
  `dml-invalid-regression-20260806/`、`dql-invalid-regression-20260806/`、
  `tcl-invalid-regression-20260806/`。
- [x] validator 256 cases、Python unittest 120/120、全部 SQL matrix shell `bash -n`、
  `cargo test -p postgresql_protocol` 15/15、`cargo check -p runtime_gateway --tests` 与
  `git diff --check` 通过；Docker SQLT 容器无残留。
- [x] `cargo test -p mysql_protocol` 的本功能无关基线结果为 37/52 通过、15/52 失败：7 项依赖未启动
  本机服务或争用固定端口，6 项 auth mock 超时，另有 2 项既有握手 fixture 断言差异。
  本批不修改这些旧测试；真实 MySQL Docker 的 invalid direct/gateway 20/20 路径已经覆盖本功能错误映射。

### SQLT-3F2 开发方案（2026-08-06）

目标：新增 `SQLT-INVALID-014`～`021` 八条协议、词法和资源边界 case，使 invalid/unsupported 达到
22/30。先用固定 Docker direct 路径校准 vendor/wire 契约，再验证 security-off gateway；不预设所有
恶意形态都必须报错，安全地作为参数或普通文本处理的 case 应精确验收返回值和零副作用，畸形 frame、
parser/resource 拒绝则精确验收错误或断连及恢复行为。

- [x] `SQLT-INVALID-014` 使用 MySQL binary protocol 发送参数数量不足、数量过多和非法类型 payload；
  `SQLT-INVALID-015` 使用 PostgreSQL extended Parse/Bind/Execute/Sync 发送参数数量不足、数量过多和类型
  不匹配。两条都在同一物理连接上发送后续合法 execute，分别检查 MySQL error identity、PostgreSQL
  SQLSTATE、ReadyForQuery=`I`、连接可复用和 canary 不变；若 connector 在发包前拦截，改用现有握手后的
  raw socket helper，不把客户端校验冒充 gateway 验收。
- [x] `SQLT-INVALID-016` 验证一个 frontend message 中的多语句边界和部分执行 canary；
  `SQLT-INVALID-017` 覆盖行注释、块注释、注释内分号及注释边界 token；`SQLT-INVALID-018` 将引号、
  反斜杠、分号和注释标记作为 binary/extended bind 值发送。direct 与 gateway 均保存逐事件结果，明确
  区分“语句被拒绝”和“payload 被安全当作数据”，禁止只检查最终退出码。
- [x] `SQLT-INVALID-019` 校准双方言超长标识符的拒绝、截断或可寻址行为；`SQLT-INVALID-020` 使用固定
  64 层括号和 2,048 项 `IN` 列表验证 parser 复杂度边界；`SQLT-INVALID-021` 对 PostgreSQL frontend
  发送恰好 16 MiB 与 16 MiB+1 的 Parse/Query message，验证边界内完成、边界外 fail-closed、旧连接状态
  与新连接恢复。规模由 SQL 文件中的 `-- @generate` 指令确定，runner 流式生成到外部 cache，不在仓库
  提交巨型 SQL，也不根据宿主内存动态放大。
- [x] 新建 `boundary-oracles.json`、`boundary_client.py`、`select_boundary_cases.py` 和
  `run-boundary-corpus.sh`。oracle 按 `case × dialect × direct/gateway` 固定 result class、error identity、
  recovery、transaction status 和 before/after state；runner 每条重建 fixture，保存 raw/normalized wire
  事件、生成参数、hash/byte count、gateway log、`results.jsonl` 与 summary，并在退出时清理全部进程和
  Compose 资源。
- [x] 调整 invalid validator 的 corpus ownership，使 3F1 deterministic oracle 与 3F2 boundary oracle
  各自严格闭合且并集无遗漏、无重复；校验生成规模固定且有上限、SQL 路径不越界、frontend/protocol 与
  flow 匹配、错误/恢复/副作用字段禁止模糊值。增加 selector、生成器、wire decoder、schema 负例和
  16 MiB 边界 Rust 单测。
- [x] 验收先跑 8 条 direct/gateway 聚焦 Docker，再回归 3F1 invalid 44/44、prepared 8/8、extended
  8/8、DQL 141/141、DML 64/64、TCL 40/40 path；validator、Python unittest、shell `bash -n`、
  Rust 1.94.1 聚焦测试和 `cargo check -p runtime_gateway --tests` 必须通过。仍按“方案提交 → 功能提交 →
  todo 证据提交”形成独立 commit。

### SQLT-3F2 验收记录（2026-08-06）

- [x] 功能提交为 `1481a1b`。manifest 为 264 条，invalid 为 21 条，invalid/unsupported 合计
  22/30；新增 `SQLT-INVALID-014`～`021` 均为带四行注释的独立 SQL 文件，大载荷仅以固定
  `-- @generate` 参数生成并在证据中保存 byte count 与 SHA-256。
- [x] 固定 MySQL 8.0.42 与 PostgreSQL 16.8 Docker 完整验收 12 case-dialect、24/24 direct/gateway
  路径通过；证据位于外部 cache 的 `boundary-acceptance-20260806-01/`。最终收紧 PostgreSQL Bind
  错误分类后强制重建 gateway 并定向复验 2/2，证据位于 `boundary-pg-bind-final-20260806-01/`。
- [x] 修复 MySQL 未知 binary Bind 类型经 unsafe 转换导致进程 abort，以及 PostgreSQL malformed Bind
  直接断连两项缺陷；未知/截断 MySQL frame 现返回协议错误，PostgreSQL extended 协议错误返回 `08P01`、
  等待 Sync 后 ReadyForQuery 并保持连接，backend/configuration 错误及 16 MiB+1 frame 仍 fail-closed。
- [x] 相邻 3F1 invalid 44/44、prepared 8 case-dialect/16 path、extended 8 case-dialect/16 path、
  DQL 141/141、DML 64/64、TCL 20 case-dialect/40 path 回归通过；证据分别位于外部 cache 的
  `invalid-regression-3f2-20260806-01/`、`prepared-regression-3f2-20260806-01/`、
  `extended-regression-3f2-20260806-01/`、`dql-regression-3f2-20260806-01/`、
  `dml-regression-3f2-20260806-01/`、`tcl-regression-3f2-20260806-01/`。
- [x] validator 264 cases、Python unittest 133/133、SQL matrix shell `bash -n`、Rust 1.94.1
  `cargo check -p runtime_gateway --tests` 与 `git diff --check` 通过，Docker SQLT 容器无残留。

### SQLT-3F3 开发方案（2026-08-06）

目标：新增 `SQLT-UNSUPPORTED-002`～`009` 八条危险能力和方言 unsupported case，与既有
`SQLT-UNSUPPORTED-001` 共同形成 9 条 unsupported corpus，使 invalid/unsupported 达到 30/30。
现有跨协议 translation reject 仅覆盖 translation policy，不能保护同协议 security-off 路径；本批先建立
协议无关、同协议和跨协议都生效的 backend execute 前置 guard，再以固定 Docker 校准 vendor 权限错误并
验证 gateway 稳定返回 unsupported。guard 的错误和审计只保存 capability code，不回显 SQL、程序文本或路径。

- [x] 定义保守 SQL token scanner，跳过行/块注释、单引号、quoted identifier 和 PostgreSQL dollar quote，
  仅根据语句 token 分类危险能力，避免字符串/注释中的 `COPY`、`LOAD DATA` 等误报。guard 覆盖 PostgreSQL
  `COPY ... PROGRAM`、server-side file `COPY`、`DO`、`CALL`，MySQL `LOAD DATA`、`INTO OUTFILE/DUMPFILE`，
  PostgreSQL `VACUUM/ANALYZE`，双方言已知 cross-dialect 语句，以及 `ALTER SYSTEM`、`SET GLOBAL/PERSIST`、
  `SET SESSION AUTHORIZATION/ROLE` 等特权 session/system 设置。
- [x] 在 runtime command decode 后、route/plugin/PDP/backend execute 前调用 guard；命中时编码稳定的
  `unsupported_operation` 响应、记录低基数 capability 和 outcome，并继续保持连接可用。增加 Rust 聚焦测试，
  使用计数 backend 证明命中 case 的 execute 次数为 0，同时覆盖 comments/literals 不误报和每个 capability
  的双方言分类；不把高风险语句依赖于 security policy 是否开启。
- [x] 新增 8 个带 `case / Purpose / Expected / Dialect` 四行注释的独立 SQL 文件：server-side file COPY、
  PostgreSQL DO、CALL、MySQL LOAD DATA、INTO OUTFILE、VACUUM/ANALYZE、双方言 cross-dialect、双方言
  privileged catalog/session 设置。多动作 case 用固定 `-- @step` 分隔；程序/文件 case 使用不可达 sentinel，
  不包含宿主路径、凭据或真实命令。既有 `001` 纳入同一 runner，任何 direct 危险校准前必须断言 PostgreSQL
  `sqlt` 非 superuser、MySQL `sqlt` 无 FILE/SYSTEM_VARIABLES_ADMIN 等权限。
- [x] 新建 `unsupported-oracles.json`、selector/client/runner。oracle 按 `case × dialect × direct/gateway`
  精确固定 vendor error identity 或 gateway unsupported capability、同连接 recovery 和 before/after
  canary/catalog/session probe；禁止 `any` 和文本模糊匹配。runner 每 case 重建 fixture，证据仅写入外部
  cache，保存 normalized result、权限 preflight、state diff、gateway log、`results.jsonl` 和 summary，退出时
  清理 gateway、Compose 和临时资源。
- [x] 扩展 validator，使 unsupported manifest/oracle 严格闭合到 `001`～`009`，校验 SQL 路径、方言、
  frontend/protocol、flow、side_effect、精确 expected 和敏感 payload 禁止项；增加 scanner、selector、client、
  oracle schema 负例测试。先完成 direct calibration，再跑完整 12 case-dialect/24 path unsupported 验收。
- [x] 最终回归 3F1 invalid 44/44、3F2 boundary 24/24、prepared 16 path、extended 16 path、cursor 18 path、
  DQL 141/141、DML 64/64、TCL 40 path 与 DDL 专项；validator、Python unittest、全部 SQL matrix shell
  `bash -n`、Rust 1.94.1 聚焦测试、`cargo check -p runtime_gateway --tests` 和 `git diff --check` 必须通过。
  仍按“方案提交 → 功能提交 → todo 证据提交”形成独立 commit，生产验证继续暂缓。

### SQLT-3F3 验收记录（2026-08-07）

- [x] 方案、runtime 与 corpus/runner 分别提交为 `de7037e`、`6c64a14`、`857f551`。guard 在
  route/plugin/PDP/backend execute 前拒绝 9 类低基数 capability；PostgreSQL extended Parse 在
  `ParseComplete` 前拒绝并等待 Sync 恢复，MySQL/PostgreSQL 同连接均可继续执行 `SELECT 42`。
- [x] 固定 MySQL 8.0.42、PostgreSQL 16.8 Docker 正式验收为 12 case-dialect、24/24 path；runner 在危险
  SQL 前断言 MySQL `sqlt` 无 FILE/SUPER/system-variable 特权、PostgreSQL `sqlt` 为从出生即受限角色。
  direct vendor error 与 gateway capability oracle 精确闭合，before/after catalog/session canary 全部相等。
- [x] gateway JSON 日志的 17 个危险 SQL step 与 17 个 capability audit event 多重集合完全一致，日志不含
  sentinel、文件路径、原始 SQL 或特权对象 payload。正式证据位于外部 cache 的
  `unsupported-acceptance-20260807-02/`，Docker 容器、网络、卷与 gateway 进程均无残留。
- [x] validator 272 cases、Python unittest 155/155、15 个 SQL matrix shell `bash -n`、`git diff --check`、
  Rust 1.94.1 scanner 3/3、runtime dangerous 聚焦测试和
  `cargo check -p gateway_core -p runtime_gateway --tests` 全部通过。
- [x] 相邻回归通过：3F1 invalid 44/44、3F2 boundary 24/24、prepared 8 case-dialect/16 path、extended
  8/16、cursor 8/18、DQL 141/141、DML 64/64、TCL 20/40、普通 DDL 158/158、database DDL 10/10、
  临时表 DDL 4/4。证据目录统一使用 `*-regression-20260807-3f3/` 后缀，位于外部 SQL matrix cache。
- [x] 本批真实覆盖限定为 MySQL text、PostgreSQL simple、direct/security-off gateway；没有把 PostgreSQL
  extended、HTTP portal、policy allow/deny 或跨协议组合伪报为 SQLT-3F3 验收，这些继续由 SQLT-4/5 完成。

## 10. SQLT-4：协议与跨协议矩阵

### 10.1 Frontend 和 backend 组合

- [x] 覆盖 MySQL frontend → MySQL backend。
- [x] 覆盖 PostgreSQL frontend → PostgreSQL backend。
- [ ] 覆盖 MySQL frontend → PostgreSQL backend。
- [ ] 覆盖 PostgreSQL frontend → MySQL backend。
- [ ] 对高风险 case 全量执行四个方向，其余 case 使用可审计 pairwise。

### 10.2 协议入口

- [x] 覆盖 MySQL text。
- [x] 覆盖 MySQL binary prepared。
- [x] 覆盖 PostgreSQL simple。
- [x] 覆盖 PostgreSQL extended。
- [ ] 覆盖 HTTP portal。
- [ ] 覆盖 xproto。

### 10.3 高风险全量组合

- [ ] 全量覆盖 prepared 参数和参数改写。
- [ ] 全量覆盖 transaction 和错误恢复。
- [ ] 全量覆盖 cursor 和 portal 生命周期。
- [ ] 全量覆盖 DDL/DML 副作用。
- [ ] 全量覆盖 wildcard、列 ACL 和复杂 AST。
- [ ] 全量覆盖 mask 和 row filter。
- [ ] 全量覆盖 portal streaming。
- [ ] 全量覆盖错误码、事务状态和 ReadyForQuery。
- [ ] 输出每个组合的结构化结果和单 case 复现命令。
- [ ] 独立提交 `SQLT-4` 各协议批次。

### 10.4 SQLT-4A 同协议协议入口矩阵开发方案（2026-08-07）

目标：先闭合 MySQL→MySQL 与 PostgreSQL→PostgreSQL 的 4 个成熟入口，不重写已验收的 wire client 和
副作用 oracle。新增统一调度与结果索引层，顺序复用 prepared、extended、cursor、TCL、DML、DDL 六套
Docker runner；子套件仍负责参数编码、ReadyForQuery、transaction marker、catalog/data state 等深层语义，
SQLT-4A 负责证明声明的 frontend/backend/path 组合被完整执行且结果可审计、可单 case 复现。

- [x] 新增版本化 `protocol-matrix.json`，固定 suite、lane、runner、run-id 环境变量、期望 case/path 数和
  coverage tags。首批无过滤验收严格为 376 path：MySQL text→MySQL 148、MySQL binary→MySQL 16、
  PostgreSQL simple→PostgreSQL 196、PostgreSQL extended→PostgreSQL 16；direct/gateway 各 188。
- [x] 为 prepared、extended、cursor、TCL runner 增加 case from/to 过滤，与已有 DML/DDL 过滤语义统一。
  过滤不得改变默认全量行为；单 case 模式只生成 `acceptance_complete=false` 的复现证据，不能满足 376-path
  正式门禁。cursor `007` 的 terminate/eof 额外路径必须保留，不能按 case 数机械乘二。
- [x] 新增 selector/aggregator，将六种现有 summary schema 规范化为逐 path `results.jsonl`，每行固定 suite、
  case、dialect、frontend、backend、protocol、direct/gateway、variant、status、子证据目录和可直接运行的复现
  命令。manifest 声明与实际 dialect/lane 必须闭合，禁止根据文件名猜测未声明的跨协议能力。
- [x] 新增 `run-protocol-matrix.sh`，串行运行子套件以避免固定 Docker/gateway 端口冲突，产物仅写入
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/`。任一子套件失败、summary 缺失、计数不符、重复 path、
  非 passed 状态或 Docker 资源残留均失败；最终输出 suite/lane/path 汇总和单 case 复现入口。
- [x] 扩展 validator 并增加 spec、selector、aggregator、过滤边界负例单测；运行全部 Python unittest、15+
  shell `bash -n`、Rust 1.94.1 相关 `cargo check --tests` 和 `git diff --check`。正式 Docker 验收不设置 suite/case
  过滤，必须精确 6/6 suite、4/4 lane、376/376 path，并验证容器、网络、卷与 gateway 进程无残留。
- [x] 本切片只验收 security-off 下的同协议入口，不勾选 MySQL→PostgreSQL、PostgreSQL→MySQL、HTTP portal、
  xproto 或 policy allow/deny。后续 SQLT-4B/4C 分别实现跨协议与 HTTP/xproto；治理策略矩阵仍属于 SQLT-5。

### SQLT-4A 验收记录（2026-08-14）

- [x] 方案、统一矩阵、严格聚合、共享 Docker 主机资源归属和 Bash 3.2 正式聚合兼容分别提交为
  `7aef4a0`、`9116a27`、`64db19f`、`0642cc6`、`67b7c8d`。单 case smoke
  `protocol-matrix-resource-smoke-20260814-02/` 正确输出 `acceptance_complete=false`，不能冒充正式验收。
- [x] 固定 MySQL 8.0.42 与 PostgreSQL 16.8 Docker 正式验收位于外部 cache 的
  `protocol-matrix-acceptance-20260814-01/`：6/6 suite、4/4 lane、376/376 path 全部通过，direct/gateway
  各 188；prepared 16、extended 16、cursor 18、TCL 40、DML 128、DDL 158。
- [x] 顶层 `results.jsonl` 精确 376 行且全部为 `passed`；lane 为 MySQL text→MySQL 148、MySQL binary→MySQL
  16、PostgreSQL simple→PostgreSQL 196、PostgreSQL extended→PostgreSQL 16。每行保留 child evidence、
  variant 和单 case 复现命令，正式 summary 为 `acceptance_complete=true`。
- [x] 六个 gateway PID 均退出，本次 Compose project 与临时 client container 通过精确标签归属；容器、网络、
  卷和 gateway residual 均为 0。验收期间出现的一个无关并发容器只写入 `containers.concurrent.tsv`，未被
  误判为 SQLT 残留，也未放宽本次资源清理门禁。
- [x] GNU Bash 3.2.57 下全部 SQL matrix shell `bash -n` 通过；Python unittest 186/186、validator 272 cases、
  `cargo check -p data-proxy --tests`、`cargo check -p gateway_core --tests` 和 `git diff --check` 全部通过。
- [x] 验收边界仍为 security-off 同协议矩阵。MySQL→PostgreSQL、PostgreSQL→MySQL、HTTP portal、xproto、
  policy allow/deny、mask、row filter 和 portal streaming 均未由本批证明，继续留给 SQLT-4B/4C 与 SQLT-5。

### 10.5 SQLT-4B 跨协议矩阵开发方案（2026-08-14）

现状：runtime 已有默认关闭的 `translation_policy`、双向 SQL 基础改写、backend→frontend 类型映射和强制
streaming 路径；仓库也已有 MySQL→PostgreSQL、PostgreSQL→MySQL 两条 smoke。但现有 smoke 使用临时文件、
宽松文本断言和全局 `pkill`，未纳入 SQL manifest/oracle，也没有逐 case 结构化结果、精确错误身份或归属资源
审计。因此 SQLT-4B 不重写 translation runtime，先把已有能力收口为可审计、可复现、fail-closed 的正式矩阵。

- [ ] **SQLT-4B1 双向 DQL**：新增版本化 cross-protocol spec、独立 selector/oracle/runner 和双方言配置，固定
  MySQL text→PostgreSQL 与 PostgreSQL simple→MySQL 两条 lane。首批从双方言 DQL corpus 选择 12 个 case：
  constant、column/alias、NULL/boolean、decimal/arithmetic、predicate、IN/BETWEEN、aggregate、GROUP BY、JOIN、
  identifier quote、COALESCE/IFNULL 与 LIMIT/OFFSET；每个方向 12 path，共 24 path。case SQL 仍使用独立文件和
  四行注释，不把 backend 方言 SQL冒充 frontend 请求。
- [ ] oracle 必须按 `case × direction` 固定列名、客户端可见类型、行值、NULL、顺序和 row count；同时保存目标
  backend direct 对照，证明 translation 后结果等价。MySQL→PostgreSQL 至少验证 backtick、`IFNULL` 和
  `LIMIT offset,count` 改写；PostgreSQL→MySQL 至少验证 quoted identifier、boolean/numeric 类型映射。禁止
  `contains`、`any` 或仅比较进程退出码。
- [ ] 增加稳定拒绝集合：双方向 DDL、vendor-only operator/function、prepared/extended command 和未允许 statement
  必须在 backend execute 前返回 `translation_error`，同连接随后 `SELECT 42` 恢复；before/after canary 相等，
  gateway audit 只记录低基数 direction、statement kind、decision 和 code，不保存原始 SQL。
- [ ] runner 使用固定 MySQL 8.0.42、PostgreSQL 16.8 Docker fixture，单个隔离 Compose project 同时启动双库，
  gateway 配置使用 SQL matrix 固定端口且只操作自身 PID。临时 client container 写入 child run-id 标签；退出时
  精确验证容器、网络、卷和 gateway 无残留，禁止全局 `pkill`。全部证据写入
  `/Volumes/fushilu/.caches/data-nexus/sql-matrix/<run-id>/`。
- [ ] 过滤模式支持 direction 与 case from/to，输出 `acceptance_complete=false`；无过滤正式验收必须精确
  2/2 lane、12/12 case、24/24 path，`results.jsonl` 每行包含 case、frontend、backend、protocol、status、
  backend control evidence、gateway evidence、type map、rewrite tags 和单 case 复现命令。
- [ ] **SQLT-4B2 DML/事务**：B1 验收后再扩展 INSERT/UPDATE/DELETE、affected rows、constraint error、BEGIN/
  COMMIT/ROLLBACK 和错误恢复；所有写路径必须有精确最终状态 oracle。未完成 B2 前不勾选跨协议 DML/事务全量。
- [ ] **SQLT-4B3 协议边界**：独立评估 MySQL binary prepared 与 PostgreSQL extended 的参数类型/portal 生命周期；
  当前 runtime 明确拒绝 prepared，若本阶段不实现则以稳定 unsupported、零 backend execute 和连接恢复验收。
  DDL 继续 fail-closed，除非另有完整 AST rewrite 与 catalog 副作用设计。
- [ ] 门禁包括全部 SQL matrix Python unittest、validator、全部 shell `bash -n`、translation/core runtime 聚焦
  Rust 测试、`cargo check -p gateway_core --tests`、`cargo check -p data-proxy --tests`、`git diff --check` 和
  真实 Docker 双向验收。B1、B2、B3 分别形成“方案/功能/验收文档”独立提交，不提前勾选 HTTP portal、xproto
  或 SQLT-5 治理策略。

## 11. SQLT-5：治理、审计和故障矩阵

### 11.1 治理策略

- [ ] 覆盖 security off。
- [ ] 覆盖 allow。
- [ ] 覆盖 deny。
- [ ] 覆盖 column strip。
- [ ] 覆盖 mask。
- [ ] 覆盖 row filter。
- [ ] 覆盖 watermark。
- [ ] 覆盖 max rows。
- [ ] 覆盖 ticket。
- [ ] 覆盖 Remote PDP。
- [ ] 覆盖 Cedar。
- [ ] 覆盖 audit L0、L1、L2。

### 11.2 故障与资源回收

- [ ] 覆盖客户端取消。
- [ ] 覆盖客户端提前断开。
- [ ] 覆盖 backend 断开。
- [ ] 覆盖 backend 重启。
- [ ] 覆盖 backend 超时。
- [ ] 覆盖 pool exhaustion。
- [ ] 覆盖 lease 回收。
- [ ] 覆盖 Remote PDP 超时或无效响应。
- [ ] 覆盖 ticket 过期、撤销和重复使用。
- [ ] 覆盖审计存储失败。
- [ ] 覆盖长字段和大结果。
- [ ] 覆盖大 payload。
- [ ] 覆盖磁盘满或外部对象存储不可用。
- [ ] 每项验证客户端响应、backend 副作用、审计、metrics 和连接资源回收。
- [ ] 独立提交 `SQLT-5` 各治理/故障批次。

## 12. SQLT-6：CI 分层门禁和报告

### 12.1 执行层级

- [ ] 新增 parser 分组。
- [ ] 新增 command 分组。
- [ ] 新增 mysql-wire 分组。
- [ ] 新增 pg-wire 分组。
- [ ] 新增 PR 快速矩阵。
- [ ] 新增 nightly 扩展矩阵。
- [ ] 新增 release 全矩阵。

### 12.2 固定产物

每次运行的固定输出根目录为：

```text
/Volumes/fushilu/.caches/data-nexus/sql-matrix/<run-id>/
```

- [ ] 生成 `manifest.jsonl`。
- [ ] 生成 `results.jsonl`。
- [ ] 生成 `normalized-output/`。
- [ ] 生成 `audit/`。
- [ ] 生成 `metrics/`。
- [ ] 生成 `logs/`。
- [ ] 生成 `summary.txt`。
- [ ] 记录配置摘要和脱敏复现命令。
- [ ] 记录 Git revision 和 dirty 状态。
- [ ] 记录 binary hash。
- [ ] 记录 Docker image 名称和 digest。
- [ ] 失败 case 保存脱敏请求、响应、backend 对照和 diff。
- [ ] 禁止产物保存生产 secrets 或完整敏感结果。

### 12.3 覆盖预算和发布门禁

- [ ] canonical corpus 达到至少 230 条。
- [ ] PR 执行预算达到至少 350 次。
- [ ] nightly 执行预算达到至少 1,500 次。
- [ ] release 执行预算达到至少 3,000 次。
- [ ] summary 按 SQL 家族、协议、backend、策略和 outcome 报告覆盖率。
- [ ] `unknown` 数量必须为 0。
- [ ] release 遇到无原因 skip 必须失败。
- [ ] release 遇到 manifest 中未注册 SQL 文件必须失败。
- [ ] 成功路径与直连 backend oracle 等价。
- [ ] deny/unsupported 路径无 backend 副作用。
- [ ] 事务、审计、metrics、取消和资源回收断言全部通过。
- [ ] 独立提交 `SQLT-6`。

## 13. A10：Backend PostgreSQL `DECLARE ... WITH HOLD`

- [ ] **A10-1**：在无 mask、row filter、watermark、max_rows 等数据义务时，将 DECLARE、
  FETCH、CLOSE 路由到同一 backend lease。
- [ ] 保证 backend 连接在游标生命周期内不会回池或被其他请求复用。
- [ ] **A10-2**：验证无 `WITH HOLD` 游标在 COMMIT 后失效。
- [ ] 验证 `WITH HOLD` 游标跨 COMMIT 存活。
- [ ] 验证 CLOSE、Drop、Quit、客户端断连和 backend 断开均清理游标与 lease。
- [ ] **A10-3**：有数据义务时 fail-closed，或明确回落到 process-local 路径。
- [ ] 配置、日志和错误码明确显示 backend cursor 与 process-local cursor 路径。
- [ ] 禁止静默启用半成品配置。
- [ ] **A10-4**：覆盖单游标、双游标、重复 DECLARE、FETCH ALL、跨 COMMIT、重连失效、
  协议错误和 backend 故障。
- [ ] 覆盖安全配置开启和关闭。
- [ ] **A10-5**：更新 API、UI、metrics、runbook、架构文档和 honesty helper。
- [ ] 只有全部真实 backend smoke 通过后才把 `remainders.backend_sql_with_hold` 改为 true。
- [ ] 运行 default、extended、security 和 xproto 全矩阵。
- [ ] 独立提交 A10 各实现切片。

## 14. A08：PostgreSQL wire 透传与 backend TLS

- [ ] **A08-1**：明确 Streaming、事务和 pool lease 生命周期契约。
- [ ] 覆盖 PortalSuspended、多 Execute、事务和客户端断连时的 lease 归还/销毁。
- [ ] **A08-2**：覆盖完整 Parse/Bind/Describe/Execute/Sync 状态机。
- [ ] 覆盖多 Execute 连续帧、参数重写、ReadyForQuery、错误和回落路径。
- [ ] **A08-3**：对 MySQL/PG backend 校验证书、CA、hostname 和 require TLS。
- [ ] 定义 backend 不支持 TLS 时的回落或拒绝策略。
- [ ] TLS 配置错误必须 fail-closed。
- [ ] **A08-4**：区分 `passthrough_client`、`passthrough_extended`、
  `streaming_demote` 和重新编码路径。
- [ ] 检查字节计数、watermark、mask、row filter 和审计不会被透传绕过。
- [ ] 证明连接归还/释放无泄漏且无跨请求污染。
- [ ] 独立提交 A08 各切片。

## 15. A09：Portal 端到端流式

- [ ] **A09-1**：消除可以消除的 Complete 物化。
- [ ] 为无 RowStream、空结果、控制语句、INSERT 和 UPDATE 定义明确流式边界。
- [ ] 能使用 backend window 的路径不得先聚合完整结果。
- [ ] **A09-2**：验证 NDJSON、JSON 分片和 CSV 三种格式。
- [ ] 验证同协议与 PostgreSQL↔MySQL 双向跨协议的窗口、顺序、错误、响应头和结束帧。
- [ ] **A09-3**：HTTP 与 xproto 均验证窗口上限、逻辑 peak、断连取消和 lease 回收。
- [ ] 证明审计写入不会无界阻塞查询。
- [ ] **A09-4**：UI、metrics 和文档区分 `backend_window` 与 `chunked`。
- [ ] 不把 HTTP 分块或逻辑 peak 宣称为 backend 真流式或 RSS 证明。
- [ ] 独立提交 A09 各切片。

## 16. A06：Backend 到 PEP 的精确窗口内存证明

- [ ] **A06-1**：建立 Linux cgroup、`/proc` 和 macOS 可重复测量夹具。
- [ ] 固定数据规模、窗口大小、并发度、allocator 和采样周期。
- [ ] **A06-2**：证明 steady-state 峰值接近 1～2 个窗口。
- [ ] 区分 encode payload、进程 RSS、cgroup 指标和 OS/allocator 噪声。
- [ ] 断言没有完整结果集物化。
- [ ] **A06-3**：覆盖 MySQL、PostgreSQL、事务、mask、长字段、空结果和控制语句。
- [ ] 覆盖客户端提前断开和失败后的资源回收。
- [ ] **A06-4**：测量门禁稳定后再更新 remainders、metrics、UI 和架构文档。
- [ ] 在此之前保持 `remainders.process_rss_window_byte_ci=false`。
- [ ] 独立提交 A06 各切片。

## 17. H05：多实例状态与 Vault 内存边界

- [ ] **H05-1**：定义字段级并发 merge 冲突策略。
- [ ] 定义版本/时间戳、删除、重放和旧 LWW 文件兼容语义。
- [ ] 使用两个以上实例并发写入，证明不会静默丢更新。
- [ ] **H05-2**：评估并实现目标平台 `mlock` 或等价能力。
- [ ] 定义内存锁定失败策略和资源释放。
- [ ] 评估 fork、core dump、swap 和崩溃时敏感内存风险。
- [ ] 无法可靠证明时保持 `remainders.mlock=false`。
- [ ] **H05-3**：覆盖锁竞争、进程崩溃、半写文件、错误密钥和重启恢复。
- [ ] 覆盖 revoke、prune 和 Drop 后擦除。
- [ ] 编写可重复生产故障演练和 runbook。
- [ ] **H05-4**：只有实现与跨实例测试达标后更新 crdt/mlock honesty、UI 和配置模板。
- [ ] 独立提交 H05 各切片。

## 18. B08：L2 样本与大 payload 门禁

- [ ] **B08-1**：定义本地落盘和 OpenDAL 存储边界。
- [ ] 定义失败重试、保留期、访问权限、加密和删除语义。
- [ ] 上传失败不得阻塞查询或伪报成功。
- [ ] **B08-2**：使用真实大 payload 和并发验证 rows/bytes 上限。
- [ ] 覆盖磁盘满、对象存储不可用、超时和截断。
- [ ] 验证审计级别和敏感字段处理。
- [ ] **B08-3**：在模板、runbook、API、UI 和 smoke 中保持默认关闭。
- [ ] 明确高 QPS 限制、非全量 L3 和非完整结果归档边界。
- [ ] 独立提交 B08 各切片。

## 19. T01：列 ACL 与复杂 SQL

- [ ] **T01-1**：决定 wildcard allow 模式的最终策略。
- [ ] 如果实现 catalog 展开，验证 `*`、`t.*`、alias wildcard 与 schema 变化。
- [ ] 如果不展开，保持保守放行/拒绝语义并更新 honesty 字段。
- [ ] 未推导出列集合时禁止静默 strip 或隐式放行。
- [ ] **T01-2**：覆盖多层相关子查询、CTE、窗口、集合运算和函数表达式。
- [ ] 覆盖方言差异、alias 遮蔽和重复列名。
- [ ] 无法安全重写的 AST 必须 fail-closed。
- [ ] **T01-3**：覆盖 MySQL/PG、prepared/simple、跨协议 portal 和 deny/allow。
- [ ] 验证返回列集合、错误码、审计事件和敏感列不泄露。
- [ ] 更新支持 AST 清单和 `star_expands_wildcard` 等 honesty 字段。
- [ ] 独立提交 T01 各切片。

## 20. H04b-4：生产部署验收（暂缓）

本地 Docker + Keycloak + HTTPS edge 的仓库代码验收已经完成。以下项目需要真实外部环境，
但按当前决定暂缓，不阻塞本地代码开发与 SQL 测试。

- [ ] 在真实外部 HTTPS 环境部署 Data Nexus。
- [ ] 配置公共 DNS 和受信任 CA。
- [ ] 配置真实 IdP tenant、Authorization Code + PKCE 和生产回调 URL。
- [ ] 验证真实 IdP issuer、audience、expiry、JWKS、角色映射和 logout。
- [ ] 配置外部防火墙、最小开放端口和网络边界。
- [ ] 使用安全 secret 注入，禁止 secrets 进入 Git 或验收产物。
- [ ] 验证服务器和 IdP 时钟同步。
- [ ] 验证日志与审计脱敏。
- [ ] 留存脱敏部署证据、健康检查和回滚步骤。
- [ ] 在恢复生产验收前，不把 localhost Docker 结果描述为生产部署证明。

## 21. P3 后置任务（不纳入当前版本）

- [ ] **F30 敏感识别增强**：定义可审计规则、误报/漏报评估、性能预算和处理边界；
  非目标是全量 DLP。
- [ ] **P01 新协议**：评估 Redis 等协议的需求、威胁模型、兼容范围和维护成本。
- [ ] **P02 深终端 Agent**：定义部署模型、权限边界、升级和离线行为。
- [ ] **P03 审计 Parquet/分析**：评估 Parquet/DataFusion schema、冷热存储、查询隔离和成本，
  不得影响审计写入热路径。
- [ ] **P04 Sharding rewrite**：补齐路由、事务一致性、重试和故障语义设计，完整设计前
  不实现半成品。

## 22. 每个任务的通用完成标准

- [ ] 实现范围与 manifest/capability/配置声明一致。
- [ ] 未实现能力 fail-closed 或显式标记，禁止静默 no-op。
- [ ] 添加针对行为的 unit、integration 或 smoke 测试。
- [ ] 失败信息能定位到 case、协议、策略、backend 或资源边界。
- [ ] 运行相关 `cargo check`、`cargo test` 和 feature 矩阵。
- [ ] 验证默认构建、`security.enabled=false` 和 v1 L0 行为不回归。
- [ ] 适用时运行 default、all、cedar、extended、xproto smoke。
- [ ] 使用真实固定版本 MySQL/PostgreSQL Docker backend 验证 SQL 行为。
- [ ] 成功路径检查返回值、行顺序、类型、affected rows 和最终状态。
- [ ] 失败路径检查稳定错误身份、事务状态和 backend 无副作用。
- [ ] 验证 audit、metrics、连接/lease 和临时对象回收。
- [ ] 更新必要的 API、UI、metrics、runbook、架构文档和 honesty 字段。
- [ ] 所有构建和验收产物位于 `/Volumes/fushilu/.caches/data-nexus/`。
- [ ] 仓库内不生成大体积 target、数据库目录或输出产物。
- [ ] `git diff --check` 通过。
- [ ] 工作区不包含意外或无关变更。
- [ ] 每个独立功能完成独立 Git commit，标题包含任务 ID。
- [ ] 提交后将实际 commit ID 写入 `todo-impl.md`。

## 23. 当前下一任务

- [ ] 开始 **SQLT-4B 跨协议矩阵**：先盘点 runtime 中 MySQL frontend→PostgreSQL backend 与 PostgreSQL
  frontend→MySQL backend 的 translation/encode 边界，明确可支持语句、稳定拒绝语句、类型映射、错误身份、
  affected rows、事务状态和副作用 oracle；首个切片只实现可证明的最小双向 DQL，不提前覆盖 prepared、DDL、
  HTTP portal、xproto 或治理策略。
- [ ] SQLT-4B 开发前先提交详细方案，固定高风险全量 case 与其余 pairwise 选择、双向 Docker backend、
  frontend wire client、结构化 path schema、单 case 复现、资源清理、验收规模和外部证据目录。
